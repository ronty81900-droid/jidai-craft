# 次のチャットへ ── 時代クラフト 引き継ぎ書

最終更新: **2026-08-23**（プラグイン **v0.42.0** / 直近の作業は §0-1、判断待ちは §0-3）
対象: このプロジェクトを新しいチャットで引き継ぐ人（および Claude）

---

## 0. 最初にやること（この順で）

1. **§0-1（直近でやったこと）と §0-3（判断待ち）を読む**（3分）。ここだけで現在地が分かります
2. **§2 の作法4つを読む**（このプロジェクトの決まり。ユーザーからの明示の指示です）
3. **いまの状態を自分で測る**（§5 のコマンドをそのまま打つ）
4. **スキルを2つ起動する**
   - `mc-bukkit-plugin` … サーバー側（プラグイン・Arclight・1.20.1）
   - `mc-forge-clientmod` … クライアント MOD 側（Gradle 無しビルド・SRG 名・絵の差し替え）
5. **運営向けの正本は `docs/ゲームマスター説明書.md`**（当日の進行・コマンド・事故対応・罠）

> **`docs/` の3本（README / HANDOVER / plugin_README）は 1.21.10 時代の開発記録**です。
> いまは **1.20.1**。数字を写す時はルートの `README.md` か、この引き継ぎ書を見ること。

## 0-1b. 時代5「未来」を作った（v0.42.0）

**ご指示（2026-08-23）:**
> 現代へ到達は勝利条件にしません。
> 現代へ到達後、現代から未来に到達したら勝利とします（建築ブロック数と石油の数で勝利みたいな）

**時代は 鉄器 → 中世 → 近代 → 現代 → 未来 の5つになりました。**
勝ち方は5つ（`#shouri_shu` に **5＝未来到達** を足した）。

| 現代 → 未来 の条件 | 値 | どういう壁か |
|---|---:|---|
| 石油の保有 | **500本** | 「まだ持っているか」の壁。腐敗と略奪で下回る |
| 拠点の建材 | **2,400個** | 「時間」の壁。近代の 800 から +1,600 |
| 勢力の貯金 | **0円** | 要りません（ご指示に貯金が無いため） |

### ★★ 数字をこう決めた理由（実測） ★★

**石油は 550 以上にすると届きません。** `tests/clear_time.py` で測りました。

- 先頭は現代に着いた時点で **約459本**。上限は「必要量×1.2」（`jidai:sekiyu/jougen`）
- **2つめの勢力が現代に着くと中央が現代になり、全勢力から石油を40%徴収** → 約300本まで落ちる
- そこから積み直すと、500本に **+90分**、600本なら **+105分**。3時間の催しに入らない
- 石油は**湧き止まります**（中央が現代になってから 3350本＝140分ぶんで終わり）。
  腐敗（15分で10%）と釣り合う上限は約900本で、実際に持てるのは 5時間かけて 670本ほど

→ **500 は「時間の壁」ではなく「まだ持っているか」の壁**。
　**建材 2,400 が時間の壁**で、**「2番手が現代に着く前に建て切れるか」**が駆け引きになります。

**建材は 6,912 を超えられません。** 数える区画が拠点中心から **24×12×24**（`jidai:shinko/kenchiku` の clone）だからです。
超えるとどれだけ建てても永久に勝てないので、**`MiraiTest` が実物の clone の範囲を読んで見張ります**。

> **★ 建材 2,400 は実機で測っていません。** `clear_time.py` の模型は石油と貯金しか数えません。
> 20人で 15〜25分ぶんと見ています。**`scoreboard players set 進行_建築_現代 settei <値>` の1行**で変えられます。

### 触った所

| 側 | ファイル |
|---|---|
| データパック | `load`（3つの settei）/ `shinko/hantei`（時代4の枝・打ち止めを5へ）/ `sekiyu/jougen`（現代の上限）/ **承認5本**（未来へ着いたら勝ち）/ `sensou/shouri_kakutei`（5＝未来到達） |
| プラグイン | `Kane`（`JIDAI_MIRAI`・必要量の表に現代を追加・呼び名に未来）/ `Shinko`（現代からも申請できる・「承認されたら勝ち」の一文） |
| MOD | `Keiji.jidaiMei`（5＝未来） |
| 検査 | **`MiraiTest` 39項目**を新設。`bunsho_kakunin` に未来の突き合わせ（+16）。`clear_time` を時代5まで伸ばし、**進行条件を load から読む形**へ |
| 道具 | **`tests/dp_haichi.py` を新設**（データパックを実機2台へ配る。サーバー稼働中は拒む・照合する） |
| 道具 | **`tests/pdf_kakunin.py` を新設**（納品 PDF を実装と突き合わせる） |

**わざと壊して落ちることを 8通り 確認**（打ち止めを4に戻す／現代の必要量を見ない／承認が勝ちを宣言しない／
建材を区画より大きくする／石油の上限を近代のままにする／Kane が現代の欄を読まない／勝ち方5の知らせを消す／申請の上限を4に戻す）。

### ★ 納品された説明書2冊を機械で突き合わせました

`python tests/pdf_kakunin.py` → **PASS 16 / FAIL 13**。
ページ数は依頼どおり（遊び方12 / 詳細版24）で、文字も「文字」として入っています。
**中身は発注時の仕様のまま**なので、差し替えが要ります。
**そのまま送れる依頼文を `docs/Codex送信用_説明書の差し替え.md` に用意しました**（直すページを9か所 特定済み）。

---

## 0-Y. ★ 説明書2冊の依頼文を書き直した（2026-08-26）

**文言はすべて Claude が書きました。** ご指示は「遊び方＝誰でも分かる版・生成イラスト多用・
高校生向け／詳細版＝絵は流用して少なめ・社会人向け」。

| | A 遊び方 | B 詳細版 |
|---|---|---|
| ページ | **12** | **22** |
| 読み手 | 高校生 | 社会人 |
| 絵 | **24枚 すべて新規** | **A から流用。新規0枚** |
| 主役 | 絵（1ページの70%以上） | 表（絵は25%以下） |

### ★★ 前回までと逆にした所 ★★

第3回〜第7回は「**画像生成モデルを使わないこと**」とお願いしていました。
**今回はその決まりを取り下げ、「生成イラストを多用してください」に変えています。**
（ただし パレットに寄せる後処理 と、**絵の中に文字を入れない**ことは条件に付けた。
　生成モデルは日本語を正しく描けないため）

### 送る物は3つ（★ 資料も要る）

`Codex依頼文_説明書A_遊び方.md` / `_B_詳細版.md` / **`資料_商品一覧.md`**
B が「資料の §1 の表をそのまま」と指しているので、資料が無いと作れません。

### ★ 送る前に必ず走らせる

```bash
python tests/iraibun_kakunin.py
```

**依頼文そのものを実装と突き合わせます（84件）。**
数字がずれた依頼文を送ると、**間違った説明書が刷られて刷り直し**になるため。

**★ この検査で、自分の書いた検査の弱点を1つ見つけました。**
最初は「文書のどこかにその数字があればOK」でしたが、建材 2,400 を 2,000 に壊しても
**別ページの同じ数字で通ってしまいました**。**表を行ごとに読む**形に直し、
わざと壊して捕まることを確認しています。

### 一緒に直した物

- `tests/pdf_kakunin.py` のページ数 24 → **22**
- `tests/bunsho_kakunin.py` の依頼文の表の照合を `iraibun_kakunin.py` へ移した
  （二重に持つと、片方だけ直した時に黙って食い違うため）
- `docs/資料_商品一覧.md` から傭兵の店を外した

---

## 0-Z. ★ 積み残し3件のうち2件を片づけた（2026-08-26）

### ① 配布用 git（`Desktop\KUN\jidai-craft`）を今の状態へ

- `tests/git_matome.py` に **`--ue`（上書き）** を足した。`.git` は消さずに中身だけ入れ替える
- jar の名前を決め打ちしなくなった（`plugin/` でいちばん版が大きい物を拾う）
- コミット `8ffa95b`（v0.55.0）。**タグ `banzoku-youhei-ari` は無事**
  （そこから `Youhei.java` 492行がそのまま読めることを確認済み）

```bash
python tests/git_matome.py --ue
```

★ `--ue` は手書きの5本（README / LICENSE / .gitignore / .gitattributes / 外部ファイル.md）も
　消すので、そのあと `git checkout HEAD -- <その5本>` で戻すこと。

### ③ クリア想定時間を測り直した

**傭兵の取り分 20%（25人/125人）を引かなくなったので、約1時間 速くなった。**

| | 前 | 後 |
|---|---:|---:|
| 現代へ着く | 3時間12分 | **2時間13分** |
| 未来到達（勝ち） | 3時間27分〜37分 | **2時間30分〜40分** |
| 超特殊 | 3時間17分〜27分 | **2時間18分〜28分** |

**★ ついでに見つけたズレ**: 見積りの道具が**バブル（近代で貴金属×2）を入れていなかった**。
道具は 652分（10.9時間）、文書は手計算の 5.8時間 で食い違っていた。
道具に入れて **5.9時間** を直接 出すようにした（倍率と時代は `Ginko.java` から読む）。

**★ 人数の効きが小さくなった。** 傭兵が居た頃は傭兵の人数がいちばん効いた（34分 差）。
今は **100人でも70人でも 2時間13分**。60人まで減ると 20分 遅くなる。

### ② 説明書の PDF ── 下ごしらえだけ済み

**文言はユーザーが書かれる**（遊び方＝高校生向け・絵たくさん / 詳細版＝社会人向け・絵は流用）。

- `tests/setsumei_seihon.py` を作った → **`docs/説明書の中身（正本）.md`** を生成
  - 数字はすべて実装から拾い直す（手で写さない）
  - 「書いてはいけないこと」＝傭兵・蛮族・人望勝利・Uキー（どれも**止めた**仕組み。説明書に載せない）
  - 章立ての案（遊び方11ページ / 詳細版22ページ）
  - **絵の一覧 24枚**（詳細版で流用するのは 18枚）
- `docs/Codex送信用_説明書の差し替え.md` は送れる形に整えてある（項目10 に削除指示）

---

## 0-A. ★★★ 傭兵・蛮族をすべて止めた（v0.55.0・2026-08-26）★★★

ご指示:「傭兵要素はすべて削除します。内容が細かくなりすぎてしまう。
　　　　裏道は消して、戦いをメインにします。傭兵 KUN も消します。
　　　　蛮族傭兵要素はここで記録だけして、復活が必要な時は残します。
　　　　ただ今回は、5勢力のバトルをメインとします。」

### 記録（復活させたい時はここ）

| | |
|---|---|
| 仕組みの説明と戻し方 | **`docs/蛮族と傭兵（休止中）.md`** |
| 元のコード一式 | git の印 **`banzoku-youhei-ari`**（`Desktop\KUN\jidai-craft`） |

```bash
git checkout banzoku-youhei-ari -- plugin/ datapacks/ clientmod/ docs/
```

### 外した物

| どこ | 何を |
|---|---|
| **`Youhei.java`** | ファイルごと（492行） |
| `Shop.java` | 蛮族の店・`ou` の分岐 |
| `Kane.java` | 身分の見分け（`banzokuGawaKa` `youheiKa` `mibunMei` ほか） |
| `Ginko.java` | 石油 12円・蛮族お断り |
| `Kanri.java` | `tp youhei` `risu youhei`・契約の後始末・`randomAround`（誰も呼ばなくなった） |
| `Shouri.java` | **人望勝利**・特別枠の定数（2026-08-29 に記録ごと廃止） |
| `plugin.yml` | `/hire` `/yes` `/no` `/headhunt` `/youheimise` |
| データパック | `team youhei`・`youhei` の目的・`fuhai/youhei.mcfunction`・傭兵_最低額 |
| クライアント MOD | **U キー**と「蛮族の店」の見出し |
| 検査 | `BanzokuTest`(109) `Stage5Test` をファイルごと。ほか約60件 |

### ★★ 一緒に変わった物（見落とすと事故になる）★★

1. **勝ち方が5つ → 4つ**（戦争 / 経済 / 超特殊 / 未来到達）。
   **人望勝利は「傭兵の過半数を従える」だった**ので一緒に無くなった。
   **番号は 1/3/4/5 のまま飛ばしてある**（データパックの何箇所かに数字で
   書いてあり、詰めると黙って食い違うため）。

2. **石油の合計の数え方が変わった。**
   `clock.mcfunction` の `scores={youhei=0}` を外したので、
   **チームに居る人は全員 数に入る**。前は傭兵の石油だけ入らなかった。
   → **`docs/クリア想定時間.md` の見積りは古い**（3:12 は傭兵ありの数字）。

3. **`randomAround` を消した。** `tp youhei` と `risu youhei` 専用だった。

### 検査

**プラグイン 1,074 PASS / 0 FAIL**（傭兵の検査 約230件が減った）
**文書 253 PASS / 0 FAIL**（傭兵の照合 約80件が減った）

---

## 0-0. ★★ 値段を「円」表記にした（v0.52.0・2026-08-26）★★

ご指示:「販売所に値段を書いてください「円」表記で。例：チェーンの兜　勢力 15円」

**やったこと**

| どこ | 前 | 後 |
|---|---|---|
| MOD の画面（値段 43件） | `個人 2` | **`個人2円`** |
| プラグインの説明 (lore) | `値段: 個人の金 2` | **`値段: 個人の金 2円`** |
| 小さいカードの値段の枠 22件 | x=40 幅41 | **x=38 幅46** |

**★ なぜ空白を落としたか（推測ではなく実測）★**

本物の `1.20.1.jar` と資材置き場から字幅を測った（`clientmod/tests/ji_haba.py`）。
**全角 9px / 数字 6px / 空白 4px / 読点 2px。**

| | 幅 | 枠 |
|---|---:|---:|
| `個人 100円`（空白あり） | **49px** | 45px しか取れない → **切れる** |
| `個人100円`（空白なし） | **45px** | 46px → 入る |

小さいカードは「アイコンの枠の右端 (card+22)」と「カードの右端 (card+69)」に挟まれ、
**どう広げても 46px が限界**。ご指示の例は空白入りだったが、3桁の値段が
折り返して切れるため、**全部の値段から空白を落として揃えた**。
日本語は空白なしで読めるので、揃っている方が見やすい。

**★ 幅の検査を常設にした**: `clientmod/tests/haba_kakunin.py`
manifest の**全 176件**を測り、`max_lines` を超えたら落ちる。
わざと `個人123456円` に書き換えて、**本当に落ちることを確認済み**。

---

## 0-1. ★★ 監査と軽量化（v0.51.0・2026-08-26）★★

`drift-audit` で洗い、**自己採点 78/100**。見つけて直した物:

| # | 何が |
|---|---|
| 1 | **★ かまどの表示が毎tick・全件で送られていた。** `getPlayerExact` ＋ `getViewers` ＋ パケット4本を、**空のかまどでも**。125人が一度ずつ開けば 2,500回/秒 → **変化が無ければ送らない**形に（焼ける速さは1tick も変えていない） |
| 2 | 王の棚（50品）を**画面を開くたび・クリックのたび**に作り直していた → 一度 組んで使い回す |
| 3 | 死んだコード4件を削除（`Basho.torokuSumi` / `Basho.arubekiBlock` / `Seiryoku.saisenKinshi` / `Shop.kaikinIchiran`） |
| 4 | `Tatakai.java` ほか2本が文書のソース一覧に無かった → 追加 |
| 5 | **リセットが「死んで蘇っていない人の預かり」を消していなかった**（蘇った瞬間に特殊アイテムが戻る） |

### ★ 途中で自分の間違いを1つ捕まえた

最初は「空のかまどを 10tick に1回だけ見る」形にしたが、**火が点くのが最大0.5秒 遅れる**。
「振る舞いは変えない」と言った約束と違うので取り下げ、**送信だけを間引く**形に変えた。
**検査（KamadoTest 4件）が落ちて気づいた。**

**わざと壊して落ちることを 4通り 確認**（毎tick 送る／送れなくても控える／控えを0始まりに／棚を毎回 作り直す）。

---

## 0-1a. ★★ 同士討ちの禁止と、キルで10%奪取（v0.45.0）★★

**ご指示（2026-08-24）:**
> 同じ勢力の人間は殺せないようなコマンドを組んでください。
> そして、敵勢力もしくは蛮族を殺すと相手の所持金の10％奪える
> （勢力貯金からは取れないようにしてください。）
> 蛮族は仲間うちでも殺し合いができるようにしてください。

**新設 `Tatakai.java`。** 決まりは2つだけ。

| # | 決まり |
|---|---|
| 1 | **同じ【勢力】の2人は傷つけ合えない**（蛮族どうしは殺し合える＝勢力ではないので自然に素通り） |
| 2 | 人を殺すと**相手の個人の金の10%**をもらう。**勢力の貯金には1円も触れない** |

- **止め方は二重**: プラグインの `EntityDamageByEntityEvent`（矢や弾は**撃った人まで辿る**）
  ＋ データパックの `friendlyFire false`（**蛮族(youhei)だけ true**）
- ★ **TaCZ の弾がイベントに来るかは実機でしか分からない。** 保険があるので致命ではないが、
  **「味方に銃を撃つ」は実機で必ず試すこと**
- 知らせは**当人2人だけ**（全体に出すとチャットが流れて略奪や時代進行が読めなくなる）

### ★★ ついでに見つけた壊れ2件（傭兵の身分の見た目）★★

| # | 何が壊れていたか |
|---|---|
| **1** | **`[蛮族] ` の字が誰にも付いていなかった。** `/jidai youhei` は印（スコア）を付けるだけで、データパックが用意した `youhei` チームに**誰も入れていなかった** |
| **2** | **契約が切れると無所属になっていた。** 勢力チームから外すだけで `youhei` へ戻していなかったので、勢力でも蛮族でもない表示になっていた |

どちらも `Kane.YOUHEI_TEAM` を正本にして直した。`TatakaiTest` が見張る。

**検証**: `TatakaiTest` 31項目を新設（1233 PASS）。説明書 +14（333 PASS）。
**わざと壊して落ちることを6通り 確認**（蛮族どうしも守る／割合を50%に／味方から奪う／
蛮族も friendlyFire false／契約後に戻さない／矢の撃ち手を辿らない）。

---

## 0-1z. ★★★ 蛮族に勝利条件は無い。これは設計 ★★★

**ご指示（2026-08-24）:**
> 蛮族側に勝利はありません。しいて言えば、「勝ち馬に乗れ」ってだけです。
> 5勢力戦争とciv6モチーフなので。civ6上で蛮族側に勝利はありませんゆえ。
> 自由で縛られず、動画にも爪痕を残せる立ち位置です。取引など。

**穴ではありません。埋めないでください。**
（前の会話で私が「蛮族の勝ち筋を足しては」と書いたのは誤りです。取り下げました）

| | |
|---|---|
| 勝ち | **無い。** しいて言えば「**勝ち馬に乗れ**」 |
| 面白さ | **自由で縛られない** |
| 見せ場 | **動画に爪痕を残せる**（勝敗の外側にいるので、何をしても筋が通る） |
| 稼ぎ | **取引** |

**`BanzokuTest` の E 章が見張ります**（勝ち方が5つのままか／宣言に蛮族が出てこないか／
手渡しを止めていないか）。**わざと勝ち方を6つめに足して落ちることを確認済み。**

### 取引が本当に成立することを実装で確かめました（2026-08-24）

- **手渡しは自由**。`PlayerDropItemEvent` / `EntityPickupItemEvent` を拾っている所は1つも無い
- **石油は預けて初めて勢力の数に入る**ので、蛮族が渡した石油を勢力の人が預ければ勢力の物になる
- **石油を金に換えられるのは蛮族・傭兵だけ**（1本12円 / 勢力は8円）
- **金そのものは渡せない**（個人の金はスコア。送金コマンドも無い）→ **石油が事実上の通貨**

> **★ 実装から出てくる取引の筋（止めていません）**
> 勢力が蛮族に石油を払う → 蛮族が12円で換金 → **蛮族の店の銃**（勢力に無い5丁）を買う
> → 手渡しで勢力へ渡す。**銃器専門店の「貯金10,000超」を通さずに銃が手に入ります。**
> 蛮族の銃は勢力の上位（M107）を超えず、払う側の進行も遅れるので、割に合うかは場合による。
> **これこそ取引なので、止めていません。**

---

## 0-1a. 蛮族／傭兵と、稼ぎ口の直し（v0.43.0）

**ご指示:**
> 傭兵の方で壊れていないか確認してください。傭兵であるメリットが必要ですので。
> 傭兵は雇われる前は蛮族って名前にしましょう。雇われたときに傭兵になる感じです。

### ★★ 壊れていました。2件 ★★

| # | 何が壊れていたか | 直し |
|---|---|---|
| **1** | **雇用料の石油を、どこでも換金できなかった。** 銀行は傭兵お断り／「石油を売る」は銀行にしか無い（v0.40.0 で足した）／石油は勢力の合計にも入らない（`clock.mcfunction` が `scores={youhei=0}`）／そのうえ腐敗で減る。**報酬が「腐る物」でしかなかった** | **蛮族の店「売る」に【石油を売る】を新設**。`Ginko.sekiyuUruHontai` を共有 |
| **2** | **雇われている間は店が閉まり、銀行も使えない** → 契約の60分のあいだ**1円も稼げなかった**（その間も腐敗で減る） | **雇われても店を開いたままにした**。雇い主の拠点の販売所も今までどおり使える |

### メリット（ご指示「傭兵であるメリットが必要」）

**石油は蛮族・傭兵だけ 1本 12円 で売れます**（勢力の人は銀行で 8円）。
勢力にとって石油は時代を進める資源なので売りません。**石油を金に換えられるのは蛮族・傭兵だけ**、
という役どころにしました。正本は `Ginko.SEKIYU_NEDAN_YOUHEI`。

### 蛮族と傭兵

**同じ人です。違いは「雇われているか」だけ。**

| | 呼び名 | チーム | 頭に付く字 |
|---|---|---|---|
| 雇われていない | **蛮族** | `youhei` | `[蛮族] ` |
| 勢力に雇われている | **傭兵** | 雇い主の勢力 | 勢力の物 |

- 印（`youhei` スコア）は雇われても **1 のまま**。判定は `Kane.mibunMei`（チームに入っているか）
- **★ 内部の名前は1つも変えていません**: スコアの目的 `youhei` / 保持者 `傭兵_最低額` /
  コマンド `/hire` `/yes` `/headhunt` `/youheimise` `/jidai youhei` / クラス `Youhei` / `Shop.YOUHEI`
  （データパックとの取り決めなので、変えると黙って壊れる。`BanzokuTest` が見張る）
- **「傭兵の店」→「蛮族の店」に改名。** ★ `Shop.java` と MOD の `Tsunagu.java` の**両方**にあり、
  1文字でも違うと **MOD 側だけ素のチェスト画面**になる（サーバーは正しく動くので実機で見るまで気づけない）

**検証**: `BanzokuTest` 30項目を新設（1179 PASS）。説明書の突き合わせ +9（308 PASS）。
**わざと壊して落ちることを4通り 確認**（売値を勢力と同じにする／石油の枠を外す／雇われたら閉める／MOD の店名だけ古いままにする）。

---

## 0-1c. ★★ 人数を変えて測れるようにした / 傭兵を模型に入れた ★★

**ご質問「各勢力12・傭兵20 だとどれくらいか」に答えるために、`tests/clear_time.py` を直しました。**

```bash
python tests/clear_time.py --ninzu 12 --youhei 20
```

### ★ そのとき見つかった、これまでの見積りの誤り

`clock.mcfunction:53` は `@a[team=kyuryo,scores={youhei=0}]` で数えています。
つまり **傭兵の石油は、雇われていても勢力の合計に一切 入りません**
（`sekiyu/azukeru.mcfunction` にも「傭兵の石油は sekiyu_gokei に混ざらない」と書いてある）。

**それまでの見積りは傭兵をまるごと無視していました**（`docs/クリア想定時間.md` §8 に断り書きがあった）。
入れた結果、**現代到達 2時間13分 → 3時間12分**。文書の数字はすべて直しました。

### 人数ごとの現代到達（ふつう・先頭の勢力）

| 顔ぶれ | 1勢力の稼ぎ | 近代 | 現代 |
|---|---:|---:|---:|
| 20人×5 ＋ 傭兵25（125人） | 766円/分 | 1時間08分 | **3時間12分** |
| 12人×5 ＋ 傭兵20（80人） | 460円/分 | 1時間11分 | **3時間28分** |
| 12人×5 ＋ 傭兵10（70人） | 460円/分 | 1時間04分 | **2時間54分** |
| 14人×5 ＋ 傭兵0（70人） | 536円/分 | 52分 | **2時間13分** |

- **いちばん効くのは傭兵の人数。** 同じ 12人×5 でも 傭兵20 → 3:28 / 傭兵10 → 2:54（**34分 差**）
- **貯金は壁ではない。** `進行_貯金_*` を1割にしても1分も変わらない（測定済み）。**壁は石油**
- 3時間に収めるつまみは `腐敗_減少率` 10→5（125人 2:28 / 80人 2:40）。表は `docs/クリア想定時間.md` §3

---

## 0-1d. その前にやったこと（v0.40.0 → v0.41.0）

**ご指示: ①超特殊勝利は「案C＋D」 ②経済勝利は 500,000 のまま ③説明書A・Bは発注済み
④ルールブックは「実装どおり」に直す。** ①②④を実装しました。実機2台へ配置済み（旧 0.40.0 は削除）。

| # | やったこと |
|---|---|
| 1 | **案C**: `Gacha.KEIHIN` の**近代**から「蒸気機関の歯車」「月の石」を外した（重み 50×2 は原木へ。合計は 10000 のまま）。**5種そろうのは現代のガチャだけ** |
| 2 | **案D**: 略奪が成立した時、相手勢力の**リーダー**が持つ「集める5種」の紙を1枚 奪う（`Shouri.ryakudatsuDeUbau`） |
| 3 | **ルールブックのプロンプトを「実装どおり」へ組み替えた**（ズレ13項目を解消。`docs/ルールブック_プロンプト_生成AI絵版.md`） |
| 4 | 想定時間の計算（`tests/clear_time.py`）を**景品表から読む形**へ。焼き込みが原因で「近代で5種そろう」と言い続けていた |
| 5 | `docs/資料_商品一覧.md` の景品表を**実装から作り直した**（`tests/shiryou_gacha.py` を新設）。8-23 の 0.5% 変更の時点で**すでにズレていた** |
| 6 | 検査を **+84項目**（`RyakudatsuTest` 33 / 資料の突き合わせ / ルールブックの突き合わせ / 案C の3件）。1,934 → **2,018**（サーバー不要ぶん） |

**超特殊勝利: 57〜67分 → 3時間17分〜3時間27分。** 現代到達（3時間12分）より 10分 遅くなり、**最速ではなくなりました。**

### ★★ この会話でいちばん重い発見 ★★

**「現代へ到達したこと」自体は、実装上の勝利条件ではありません。**
`sensou/shouri_kakutei.mcfunction` の `#shouri_shu` は **1=戦争 / 2=人望 / 3=経済 / 4=超特殊** の4つだけで、
**時代到達の判定はどこにもありません。**（この引き継ぎ書の §4 が「時代到達・人望・経済・特殊」と
書いていたのは**誤り**でした。直してあります）

つまり「いちばん早く現代に着いた勢力が勝ち」は**運営が口で宣言するルール**です。
案C を入れた結果、**超特殊勝利は現代到達の 5〜15分 後**になったので、次の判断が要ります（§0-3 の #1）。

### ★ 検査が2回、自分を欺いていた（記録として残す）

1. **`indexOf` をそのまま比べる並び順の検査は、片方を消すと素通りする**（-1 < 何か は必ず真）。
   実際に「受け取れるかを先に見る」を消して**落ちなかった**。`saki()` を作って両方あることを先に見る形へ直した。
2. **わざと壊すスクリプトに `assert` を書かなかったため、置換が空振りしたのに「壊した」と表示された。**
   その状態で「検査が落ちなかった」と誤読しかけた。**壊す側にも assert を書くこと。**

---

## 0-2. その前の会話（2026-08-23）でやったこと

**プラグインは v0.37.0 → v0.40.0。** 実機2台へ配置済み（古い版は削除済み）。

| # | やったこと | 版 |
|---|---|---|
| 1 | Codex 第6回の納品（遺物11種の絵・案A）を受け入れ、MOD へ組み込み | — |
| 2 | 実機の指摘4件（十連の停止・遺物の説明・アイコン・リセット） | **0.38.0** |
| 3 | クリアアイテムの確率を全時代 **0.5%** にそろえた（遺物は据え置き） | **0.39.0** |
| 4 | 経済の救済（**バブル ×2** / **石油を1本8円で売る**）と、**傭兵の銃器を別系統**に | **0.40.0** |
| 5 | **クリア想定時間**を実装の数字から計算（`docs/クリア想定時間.md` / `tests/clear_time.py`） | — |
| 6 | **説明書3本**（GM版はこちらで作成／参加者向け A・B は Codex 依頼文） | — |
| 7 | **ルールブックのプロンプトを「完全 生成AI絵」版へ改変** | — |

**★ いちばん重い発見**: 十連が2回目から止まっていた真因は、画面を開き直す時の
`InventoryCloseEvent` を `Gacha.tojita` が「途中で閉じた」と取り違え、
**まだ1枚もめくっていない結果を先に配ってしまう**ことでした。配ると控えが消えるので、
直後のめくり処理が「もう片づいている」と判断して止まります。
初回だけ動くのは、その時 閉じるのがガチャ画面（27枠ではない）で枠数の条件に当たらないからです。

---

## 0-3. ★★ 判断待ち（いまはこの1つ）★★

| # | 何を決めるか | 今どうなっているか | 決めたら |
|---|---|---|---|
| **1** | **説明書A・B の差し替えを Codex へ送るか** | 実物の PDF を機械で突き合わせ済み（**FAIL 13**）。直すページを9か所 特定し、**そのまま送れる依頼文**を `docs/Codex送信用_説明書の差し替え.md` に用意した | 送るだけ。届いたら `python tests/pdf_kakunin.py` で 0 FAIL になるか見る |

### 実機で確かめてほしいこと（機械では見られない）

- **建材 2,400 が長すぎ／短すぎないか**（`docs/実機確認リスト.md` の §F）。
  ここだけは1回まわさないと分かりません。`進行_建築_現代` の1行で変えられます
- **未来へ進む申請 → 承認 → 「★ 未来到達勝利 ★」**が出るか

### 片づいたもの（記録）

| 前の判断待ち | どうなったか |
|---|---|
| #1 超特殊勝利が速すぎる | **案C＋D を実装**（v0.41.0）。57〜67分 → 3時間17分〜3時間27分 |
| #2 経済勝利が届かない | **500,000 のまま**というご判断。実装は触っていません（3時間では届きません） |
| #3 説明書A・B の発注 | **発注済み**。訂正は上のとおり |
| #4 ルールブックの方針 | **「実装どおり」を実装**（ズレ13項目を解消） |
| #5 第6回の B案・C案が zip に無い | A案で 124項目 PASS のまま。見比べたい時に Codex へ再送を頼めばよい（急ぎではない） |

---

## 1. これは何か

**50人規模の Minecraft イベント企画。** 5つの勢力が中央の石油を奪い合い、
鉄器 → 中世 → 近代 → 現代 と時代を進める。いちばん早く現代に着いた勢力が勝ち。

構成は4つ。**どれか1つだけ直しても動かない**ので、必ず対応関係を確かめる。

| 部品 | 場所 | 役目 |
|---|---|---|
| **Bukkit プラグイン** | `plugin/` | GUI（販売所・ガチャ・銀行 等）・買い物・運営コマンド |
| **データパック** | `datapacks/jidai_craft/` | 状態と判定（時代・戦争・石油・占領） |
| **マップ** | `map/` | 地形と拠点 |
| **リソースパック** | `resourcepack/` | チェスト画面の見た目。**UI の本命ではなくなりました**（下の §6-A） |
| **クライアント MOD** | `clientmod/` | **買い物画面を自分で描く。UI はこちらへ移しました**（下の §6-A） |
| 実機サーバー | `jikki_mod/` | Arclight。**これを起動する** |
| Paper 版 | `jikki_1201/` | MOD 無しの控え |

**プラグインとデータパックの取り決めの正本は `datapacks/CONTRACT.md`。**
どちらを直す時も、先にここを読む。片方だけ変えると黙って壊れる。

---

## 2. ★★ 作法（このプロジェクトの決まり） ★★

**この4つは、ユーザーからの明示の指示です。守ること。**

### 作法1 ── 記憶で書かない
API も版もコマンドも定数も、**実物を開いて確かめてから書く**。
jar を開く、javadoc を読む、サーバーに打つ。
**そして「どうやって確かめたか」を必ず報告に書く。**

### 作法2 ── 実物で検証する
「コンパイルが通った」で止めない。
**測れないものは「測れない」と正直に言う。** 測ったふりをしない。

### 作法3 ── 読める形で書く
- クラスとメソッドに日本語のコメント。**なぜそうしたか**を書く
- ファイル数を最小に
- **デザインパターンを使わない**
- **依存を足さない**
- 「**動くことより、読めることを優先する**」

### 作法4 ── 範囲を広げない
頼まれていないことを足さない。気付いたことは「範囲外の気付き」として報告する。

### 報告の型（毎回この順で）
1. 確かめた事実と、その確かめ方
2. 検証の結果（件数で）
3. 判断したこととその理由
4. 必要な Java の知識
5. 踏んだ罠
6. 範囲外で気付いたこと

### 既存のものの扱い
**「75点以下なら直す。90点以上ならそのまま採用」。**
自分で採点して、直したもの・そのまま採ったものを一覧で出す。

### 言葉づかい
敬体。**結論を先に**。へりくだらない。
**指示書の前提が間違っていたら、そう言う。** 黙って合わせない。

---

## 3. ★★ 触ってはいけないもの ★★

| | |
|---|---|
| `jikki_mod/` `jikki_1201/` `plugin/testserver/` | **ユーザーの本物のフォルダ。** 中の `world` `plugins/JidaiCraft` `logs` は絶対に消さない |
| ファイルを消す前 | **`netstat -ano \| grep 25565` でサーバーが動いていないか先に見る** |
| 消してよいもの | **自分が作ったファイルだけ** |
| `jidai_craft_v1_sankou` / `jidai_craft_v6_1_21_10_sankou` | **絶対に入れない。** 同じ名前空間で、後から読まれると今の仕組みが丸ごと無効になる |

> **過去の事故**: `netstat` を先に見ずに作業し、`jikki/` の Paper 設定 6 ファイルを
> 上書きして戻せなくした。同じことを繰り返さない。

---

## 4. いまの状態（2026-08-23 実測）

### 動いているもの

| | |
|---|---|
| **プラグイン** | **v0.55.0**（`JidaiCraft-0.55.0.jar`）。`jikki_mod` / `jikki_1201` の両方へ配置済み。**古い 0.54.0 は削除済み**（同じプラグインが2つあると読み込みで問題になる。控えは `plugin/` に残っている） |
| **クライアント MOD** | `JidaiUI-0.1.0.jar`。`Desktop/実機演習 1.20.1/mods` へ配置済み。**遺物11種の絵まで入っている** |
| データパック | `jikki_mod/world/datapacks/jidai_craft`（`pack_format: 15`） |
| 同居 MOD | TaCZ 1.1.8-hotfix / Simple Voice Chat 2.6.22 |
| 勝利条件 | **5つとも実装済み**（戦争・人望・経済50万・超特殊＝特殊5種・**未来到達**）。★ **「現代へ着くこと」は勝ちではありません**。現代から**未来**へ進めた勢力が勝ちます（`#shouri_shu` の 5） |
| 機械確認 | **合計 2,761項目**（サーバー不要の9種＝**2,259項目**は 2026-08-26 に実走した値。★ 残り 502 はサーバーを立てる5種で、**この環境では走らせられない**） |

> **稼働確認は `netstat -ano | grep 25565` の LISTENING の行だけ**を見ること。
> TIME_WAIT は閉じた接続の残骸で、稼働中の意味ではありません。

### 想定人数（ご指示・2026-08-23）

**5勢力 × 20人 ＝ 100人 ＋ 傭兵 25人 ＝ 125人。**
古い文書に出てくる「50人」は**古い数字**です。

### 実機で人の目で見たもの（2026-08-20）

銃を撃っても建築が壊れないこと / エンダーかまど / サイドバー / GUI が開くこと

### まだ人の目で見ていないもの ← **ここが残りの本体**

**一覧は `docs/実機確認リスト.md`**（マルチでしか確かめられない物だけを集めてある）。
2026-08-23 に足したもの:

- 十連 → 結果 → **「もう一度 十連」が2回目・3回目もめくられるか**
- **遺物の説明に「5種そろえると勝ち」が出ない**こと
- **遺物の絵が紙ではなく絵で出る**こと（★ **Minecraft を再起動しないと変わりません**）
- `jidai game reset` のあと、一度出た特殊アイテムが**また出る**こと
- **銀行の「石油を売る」**（中世から・1本8円・**勢力の石油は減らない**）
- **バブル**（中央が近代で貴金属が2倍。「(バブル ×2)」が出る）
- **傭兵の店の「銃器」タブ**（勢力に無い銃5丁。**買って実際に撃てるか**）

**この一覧を勝手に「確認済み」に変えないこと。** 実際に見た時だけ消します。

## 5. 検証の走らせ方

```bash
cd "$HOME/Desktop/KUN/時代クラフト"
```

| 何を見るか | コマンド | 直近の結果 |
|---|---|---|
| プラグインの中身 | `python plugin/tests/run_harness.py` | **1301 PASS / 0 FAIL**（`MiraiTest` 39 / `BanzokuTest` 109 / `TatakaiTest` 31 を含む） |
| **遺物の絵（第6回の納品・案A）** | `cd clientmod && python tests/ibutsu_kakunin.py` | **124 PASS / 0 FAIL**（64×64・11枚・内側の真鍮の縁まで） |
| **掘りのルートテーブル** | `python tests/horu_kakunin.py` | **34 PASS / 0 FAIL**（作るのは `tests/horu_tsukuru.py`） |
| **マルチでしか見られない物** | `docs/実機確認リスト.md` | 人が見る（機械は見られない） |
| **説明書・資料・ルールブックの数字** | `python tests/bunsho_kakunin.py` | **333 PASS / 0 FAIL** |
| **納品 PDF を実装と突き合わせる** | `python tests/pdf_kakunin.py` | 実物の PDF を読む（いまは差し替え待ちで FAIL 13） |
| **データパックを実機2台へ配る** | `python tests/dp_haichi.py` | `--miru` で差だけ見る。★ 手で cp しない |
| **資料_商品一覧の景品表を作り直す** | `python tests/shiryou_gacha.py` | 正本＝`Gacha.KEIHIN`。`--miru` で差分だけ見る（**手で写さない**） |
| **クリア想定時間の計算** | `python tests/clear_time.py` | 実装の定数から見積もる |
| 銃と弾のID | `python tests/buki_kakunin.py` | **38 PASS / 0 FAIL**（2026-08-23 に「店ごとに数える」形へ直した） |
| UI の突き合わせ | `python tests/ui_kakunin.py` | **32 PASS / 0 FAIL**（★下記） |
| データパック本体 | `python tests/verify_jidai.py` | 396（サーバーを立てる） |
| 5勢力の通し稼働 | `python tests/toushi_5.py` | 29（同上） |
| プラグイン⇄データパック | `python tests/plugin_renkei.py` | 52（同上。先に `run_harness.py`） |
| 地図 | `python tests/map_kakunin.py` | 18（同上） |
| 拠点の設置 | `python tests/setup_kakunin.py` | 7（同上） |
| **クライアント MOD** | `cd clientmod && python build.py` | **ビルドが通る**（Gradle 不要） |
| **MOD の中身** | `cd clientmod && python tests/mod_kakunin.py` | **204 PASS / 0 FAIL** |
| **特殊アイテムの絵** | `cd clientmod && python tests/tokushu_kakunin.py` | **64 PASS / 0 FAIL** |
| **特殊アイテムの絵を組む** | `cd clientmod && python tools/tokushu_moderu.py` | Shouri.java から作る |
| **MOD を実機へ入れる** | `cd clientmod && python haichi.py` | ★ 手で cp しない（下記） |
| **UI 素材** | `cd clientmod && python tests/sozai_kakunin.py` | **129 PASS / 0 FAIL** |
| **文字の表を作り直す** | `cd clientmod && python tools/hyou_tsukuru.py` | 文字141 / カード44 / タブ8 / アイテム33 / 画面8 |

### ★★ 2026-08-23: 経済の救済と、傭兵の銃器（v0.40.0）★★

| ご指示 | 実装 |
|---|---|
| **経済勝利への救済＝バブル**（近代以降は全貴金属が2倍） | `Ginko.BUBBLE_JIDAI=3` / `BUBBLE_BAI=2`。**中央の時代**で見る（勢力ごとに見ると先頭だけ先に倍になり救済にならない）。遺物「金庫」×1.2 とは掛け算で重なる（最大2.4倍）。売った知らせに「(バブル ×2)」が付く |
| **石油を換金できる**（中世以降の銀行・1本8円） | 銀行に**枠11「石油を売る」**を新設（`Ginko.SEKIYU_NEDAN=8` / `SEKIYU_URU_JIDAI=2`）。**売れるのは手持ちのアイテムだけで、預けた石油（勢力の石油）は1本も減らない**。拾うたびに「預ける（時代へ）／売る（金へ）」を選ばせる形にした。中世より前は灰色の板ガラス |
| **傭兵に別系統の銃器**（勢力と同じ銃はやめる） | `Shop.YOUHEI_JUKI` を新設し、2割引きの流用をやめた。**銃5丁＋弾4種**。タブ名は「銃」→**「銃器」**（タブは元からクリックで切り替わる） |

**傭兵の銃器（すべて個人の金・勢力と1丁も重ならない）**

| 銃 | 値段 | 解禁 | 弾 | 装弾 | TaCZ の ID |
|---|---:|---|---|---:|---|
| ルガー P08 | 600 | 中世 | 小口径 | 8 | `hamster:lugerp08` |
| ウェブリー Mk.VI | 900 | 近代 | 中口径 | 6 | `hamster:webley` |
| MP18 短機関銃 | 2,000 | 近代 | 小口径 | 32 | `hamster:mp18` |
| M1911 | 800 | 現代 | .45ACP | 7 | `tacz:m1911` |
| AKM | 3,000 | 現代 | 7.62mm | 30 | `tacz:ak47` |

弾: 小口径30発 12 / 中口径30発 16 / .45ACP30発 20 / 7.62mm30発 35。

**強さの根拠**: `TACZ_BUKI.md` の実測値で選んだ。いちばん強い AKM（威力9・毎分600発）でも、
勢力の M4A1（威力6.5・毎分810発）と秒あたりの威力はほぼ同じ。勢力の上位（M107 威力55）は超えない。

> **★★ 経済勝利は、救済しても3時間では届きません ★★**
> 稼ぎは 767円/分 → **1,566円/分**（近代以降）になり、500,000 までが **10.9時間 → 5.8時間**。
> 半分になりましたが、まだ届きません。**`経済_勝利_貯金` を 200,000 に下げると 2.6時間**で、
> 3時間の催しの終盤に決着します（`scoreboard players set 経済_勝利_貯金 settei 200000`）。
> **勝利条件そのものの変更なのでご判断ください。**

**検証**: プラグイン **1073 PASS**（1055→）/ 説明書の突き合わせ **236 PASS**（188→）。
**壊して落ちることを7通り 確認済み**（勢力と同じ銃にする / 銃の ID を打ち間違える / バブルを勢力の時代で見る /
石油の売値を変える / 売る枠の配線を外す / 手持ちを取らない / 説明書の値段をずらす）。

> **★ 銃の ID は `ShohinTest` が `TACZ_BUKI.md`（MOD の jar から作った一覧）と突き合わせます。**
> 打ち間違えると give は通るのに何も出ず、実機で撃つまで気づけないためです。

### ★★ 2026-08-23: クリアアイテムの確率を 0.5% にそろえた（v0.39.0）★★

**ご指示: 「ガチャ特殊アイテム（クリアアイテム）は 0.5%程度にとどめる。ほか効果のある特殊アイテムは据え置き」**

| | 前 | 後 |
|---|---|---|
| 鉄器 古びた護符 | 1.00%（100） | **0.50%（50）** |
| 中世 3種（護符・聖杯・海図） | 各 0.60%（60） | **各 0.50%（50）** |
| 近代・現代 5種 | 各 0.50%（50） | 据え置き |
| **遺物11種** | — | **すべて据え置き** |

浮いた重みは原木へ戻した（鉄器 原木×16 980→1030 / 中世 原木×32 1050→1080）。**各時代の合計は 10000 のまま。**

新しい割合: 鉄器 はずれ23.0 / ふつう54.3 / 金20.7 / **特殊2.0**、中世 22.0 / 52.0 / 23.0 / **3.0**（近代・現代は変更なし）。

> **★ この変更で 超特殊勝利の想定時間は変わっていません（57〜67分のまま）。**
> 最速の筋は「近代でまとめて5種を狙う」で、**近代は元から 0.5%** だったため。
> 下げたのは鉄器と中世＝「ついでに拾う」側だけ（代金 9,000円 → 10,000円）。
> **まだ効く手は `docs/クリア想定時間.md` §6 の B（0.15%へ）／C（現代限定）／D（守る危険）。**

**検証**: プラグイン 1055 PASS / 説明書の突き合わせ 184 PASS（**どちらもこの時点の数**。いまは 1073 / 236）。
**★ 説明書の検査が、この変更で実際に 10件 落ちた**（ガチャの表がずれた）ので、文書を直して緑に戻した。
検査が仕事をしている証拠なので、記録として残す。

### ★★ 2026-08-23: 説明書3本とクリア想定時間 ★★

**想定人数が確定しました: 5勢力 × 20人 ＝ 100人 ＋ 傭兵 25人 ＝ 125人**（ご指示）。

| 出したもの | 場所 |
|---|---|
| **クリア想定時間** | `docs/クリア想定時間.md`（計算は `tests/clear_time.py`） |
| Codex 依頼文A（遊び方・ほぼ全ページ イラスト・12ページ） | `docs/Codex依頼文_説明書A_遊び方.md` |
| Codex 依頼文B（詳細版・イラスト半々・24ページ） | `docs/Codex依頼文_説明書B_詳細版.md` |
| **ゲームマスター説明書**（こちらで書いた本体） | `docs/ゲームマスター説明書.md` |
| 資料（説明書を書き直す時の元） | `docs/資料_商品一覧.md` / `docs/資料_運営の裏側.md` |

**クリア想定（125人・実装の数字から計算）**

| 勝ち方 | 想定 |
|---|---:|
| **超特殊勝利**（特殊5種） | **57〜67分** ← ★最速（0.5%へそろえても変わらず） |
| （参考）現代へ着くまで | **3時間12分**（2時間11分〜届かない）。★ 現代到達は勝ちではない |
| 戦争勝利 | 実質2時間以上 |
| 経済勝利（50万） | 5.8時間（救済後。3時間では届かない） |

> **★★ 判断待ち: 超特殊勝利が本命より1時間以上 速い ★★**
> 近代（52分）に着けば、20人で十連を2〜3回ずつ回すだけで5種そろいます（代金は勢力の稼ぎ12分ぶん）。
> **説明書に書けば全勢力が狙うので、そのままだと1時間で終わる催しになります。**
> 4つの案を `docs/クリア想定時間.md` §6 に書きました（おすすめは C＋D＝現代限定にする＋守る危険を作る）。

**壁は石油。** 石油は人数に比例しません（24本/分のまま）。20人×5勢力だと金は91分で掘り切るので、
**残るのは4か所の出口を125人で取り合う形**です。腐敗（15分で10%）がいちばん効くブレーキで、
`腐敗_減少率` を 10→5 にすると現代到達が約20分 早まります。

**説明書の数字は機械で見張ります。** `python tests/bunsho_kakunin.py`（この時点 **188 PASS**。いまは **236**）が
説明書の本文の表から数字を抜き、実装（settei・定数・景品表・掘りの確率）と突き合わせます。
**わざと 2か所 ずらして落ちることを確認済み。**

#### ついでに直したズレ（説明書を書く前に潰した）

| 何が | どう直したか |
|---|---|
| `Shop.java` のコメント「手形があれば 5,000」 | 定数は **5,500**。数字をコメントに書かず `jukiShikii()` を指す形へ |
| `Gacha.java` の時代ごとの内訳コメント（はずれ2000 など） | **4時代とも実際の配列と食い違っていた**。数字を消し、「割合は GachaTest が毎回 計算する」と出所を指す形へ |
| 本書の「傭兵の店は2割引き」 | **銃と弾だけ 2割引き**。武器・食料は手書き（矢25・焼肉25＝16.7%引き、パンは2円のまま3個→4個） |

### ★★ 2026-08-23: 実機の指摘4件（v0.38.0）★★

| 指摘 | 原因と直し |
|---|---|
| **十連 → 結果 → もう一度十連 で、めくられず停止する** | **★真因: 画面を開き直す時の「閉じた」知らせを、`Gacha.tojita` が「十連の途中で閉じた」と取り違えていた。** まだ1枚もめくっていない新しい結果をその場で渡してしまい、渡すと `machi` から消えるので、直後の `mekuru` が「もう片づいている」と見なして止まる。**初回が動くのは、その時 閉じるのがガチャの画面（27枠ではない）で枠数の条件に当たらないから。2回目以降だけ壊れていた。** → `Juren` が**自分の画面**を覚え、`tojita` は**その画面を閉じた時だけ**渡す（`j.inv == inv`）。あわせて「もう一度 十連」は**クリックの処理中に開かず 1tick 待つ**（閉じるボタンと同じ理由） |
| **遺物の説明が「リーダーが5種そろえると勝ち」** | 説明文が特殊アイテム全部で1つだった。遺物は**勝利条件に入らない**ので、集めれば勝てると思い込ませる嘘になっていた。→ `Gacha.setsumeiGyou` で分けた。遺物は「〈時代〉の遺物 / 効果 / 手に入れた勢力の全員に効果が付く / 集めても勝ちにはならない」。**効果の文は `Shouri.IBUTSU_HYOU` が正本**（書き写さない） |
| **ガチャにアイコンが出ていない** | 絵が無かったのが原因（第6回の納品で解消済み）。ただし**検査は「集める5種」しか見ておらず、遺物 11種は1件も見ていなかった**。絵が無くてもサーバーは正しく動くので実機で見るまで気づけない。→ `tests/mod_kakunin.py` に**遺物 11種の通し確認**（絵・模型・紙の対応表・`Tokushu.java`・番号の重なり）を足した（**158 → 204 項目**）。★**MOD の jar を入れ替えたので、Minecraft を再起動しないと絵は変わらない** |
| **リセットで特殊アイテムの取得もリセット** | `reset zenbu` の中でしか記録を消していなかった。時代だけ戻すと「もう出た」の記録が残り、**二度目の催しで特殊アイテムが1個も出ない**。→ `reset` でも `gacha.tokushuRisetto()` を呼ぶ（遺物の効果も一緒に切れる）。**`reset zenbu` は現物も取り上げる**（`Shouri.kaishu()`）── 記録だけ消して紙が手元に残ると、リセット直後にリーダーが5種そろえたまま＝即 勝利になるため |

**検証**: プラグイン **1055 PASS / 0 FAIL**（984 → 1055）、MOD **204 PASS / 0 FAIL**（158 → 204）。
**わざと元に戻して落ちることを6通り 確認済み**（画面の同一性を見ない / 遺物にも「5種そろえると勝ち」/ 記録を zenbu の中へ戻す / 現物を取り上げない / クリックの中で開く / 王冠の番号をずらす）。

> **★ 検査が「書き方」を決め打ちしていた件**: `空き枠が足りない時、金を引く前に断っている` は
> 「金を引いた行の**次の行**が `machi.put`」という並びを見ていたため、**並べ替えただけで落ちた**。
> 見たいのは「断るのが金を引くより前か」だけなので、`mawasu10` の中だけで前後を比べる形に直した。

### ★★ 2026-08-23: 遺物の絵（第6回・案A）を組み込み ★★

- 納品 `JidaiCraft_Ibutsu_v6_3proposals.zip` → **`clientmod/nouhin/v6`**（案A を正規の道筋 `assets/jidaiui/textures/item/` と `preview/` に置き直した写し。元の manifest は `nouhin/v6_3proposals_manifest_original.json`）。
- **★ zip には案A しか入っていない。** manifest と DESIGN_SPEC は「A/B/C の3案・33枚」を申告しているが、B/C の 44 本（絵22＋プレビュー22）が欠けていた。**A で組み込んだ。** B/C を見比べたければ Codex に再送を頼む。
- 受け入れ: `tests/ibutsu_kakunin.py` **124 PASS / 0 FAIL**。依頼どおり「外周 2px の輪郭 ＋ 内側 2px の真鍮の縁」まで見る形にした。
  **★ 輪郭の層は 8近傍（斜めも隣）で数える。** 4近傍で数えると斜めの角の画素が別の層に入り、11枚すべてが落ちる（8近傍なら 0 画素の違い）。スキル `mc-forge-clientmod` の `art_check.py` にも `--neighbors 8` を足した。
- `tools/tokushu_moderu.py` は遺物を **`nouhin/v6`** から取る（集める5種は v5 のまま）。紙の対応表 16件・`Tokushu.java` 16行・`OOKISA` は遺物が 64×64。
- MOD 再ビルド → `mod_kakunin.py` **158 PASS**（この時点。翌日 遺物の通し確認を足して **204** へ）→ `haichi.py` で `Desktop/実機演習 1.20.1/mods` へ配置済み（sha256 一致）。サーバー側は変えていない。
- 人の目で見る物は `docs/実機確認リスト.md` に足した（遺物が紙ではなく絵で出る・64×64 が 16×16 に縮んで崩れない・暗い5つが並んだ時に見分けられるか）。
- 第5回の5枚を 64×64 で描き直す任意枠は来ていない（16×16 のまま。支障なし）。

### ★★ 2026-08-22: 遺物を 11種 に（v0.37.0）★★

ご指示の表どおりに作り直した。**正本は `Shouri.IBUTSU_HYOU`**。絵は `clientmod/Codex依頼文_第6回.md`（**64×64・AIっぽさを無くす**）。

| 時代 | 遺物 | 効果 | 出し方 | 確率 |
|---|---|---|---|---:|
| 鉄器 | ヒッタイトの戦車 | 移動速度 1.2倍 | スピードI を毎秒 | 0.8% |
| 鉄器 | ロンバルディアの鉄王冠 | 貴金属が出る確率 1.2倍 | 掘った時 20% で「おまけの抽選」をもう1回（`Ibutsu.horiOmake`。表は horu_tsukuru.py と突き合わせ） | 0.7% |
| 中世 | 聖剣エクスカリバー | 近接の攻撃力 +10% | 攻撃力の属性に ×1.10 の修正子（持たない人からは外す） | 0.5% |
| 中世 | テンプル騎士団の盾 | 被ダメージ −15% | `EntityDamageEvent` で ×0.85 | 0.5% |
| 中世 | 武器商人の手形 | 銃器専門店が貯金 5,500 で開く | `Ibutsu.jukiShikii` | 0.5% |
| 近代 | ベッセマー転炉 | 採掘が速い | 採掘速度上昇I | 0.5% |
| 近代 | ロスチャイルドの金庫 | 貴金属の売値 1.2倍 | 銀行の売りに掛かる | 0.5% |
| 近代 | 死の商人の手引書 | 銃器の価格 −10% | **値引きではなく戻し**（払った直後に1割 戻る。MOD の画面の値段とずれないため） | 0.5% |
| 現代 | ペニシリン | 自然回復 +20% | `EntityRegainHealthEvent`（SATIATED/REGEN だけ）で ×1.2 | 0.5% |
| 現代 | 暗視ゴーグル | 暗視 | 暗視 | 0.5% |
| 現代 | スマートフォン | 他勢力の貯金・石油・時代が見える | **`/joho`**（持たない勢力には断る） | 0.5% |

- 重みは原木から取った（鉄器 原木×16 980 / 近代・現代 原木×64 1180）。各時代の合計は 10000 のまま。
- **MOD は絵の実寸を `Tokushu.OOKISA` に持ち、16×16 に縮めて貼る**（`Mc.haruOokisa` = 11引数の blit）。16×16 の絵を貼る `haru` では 64px の絵の左上しか出ない。
- 第6回の納品は `clientmod/tests/ibutsu_kakunin.py` で点検（枚数と名前は Shouri.java から読む）。

### ★★ 2026-08-22: 遺物8種・掘りの確率（v0.36.0）★★

#### 特殊アイテムは2系統になった

| 系統 | 数 | 正本 | 勝利条件 |
|---|---|---|---|
| 集める | 5種（鉄器1・中世2・近代1・現代1） | `Shouri.TOKUSHU_HYOU` | **5種そろえると勝ち**（従来どおり） |
| **遺物** | **8種**（鉄器1・中世3・近代2・現代2） | `Shouri.IBUTSU_HYOU` | 入らない。**手に入れた勢力に効果が付く** |

| 時代 | 遺物 | 効果 | 実装 |
|---|---|---|---|
| 鉄器 | ヒッタイトの戦車 | 勢力全員の移動速度 1.2倍 | スピードI（毎秒かけ直し） |
| 中世 | エクスカリバー | 近接の攻撃力↑ | 攻撃力上昇I |
| 中世 | テンプル騎士団の盾 | 被ダメージ −20% | 耐性I |
| 中世 | 武器商人の手形 | 銃器専門店が貯金 5,000 で開く | `Ibutsu.jukiShikii` |
| 近代 | ベッセマー転炉 | 採掘が速い | 採掘速度上昇I |
| 近代 | ロスチャイルドの金庫 | 貴金属の売値 1.2倍 | `Ibutsu.uriBairitsu`（銀行の売りに掛かる） |
| 現代 | ペニシリン | 体力が自然回復 | 再生I |
| 現代 | 暗視ゴーグル | 暗視 | 暗視 |

- **効果は「勢力」に付く**（`Ibutsu.java`）。手に入れた時点で記録され、入り直しても効く。紙は戦利品で、奪われても効果は移らない。
- 記録は Gacha の `gacha_tokushu.<勢力>` の写し（別の台帳を持たない）。`reset zenbu` で消え、ポーション効果は3秒で自然に切れる。
- **勢力ごとに各1個まで**は遺物にも効く。傭兵には出ない。大当たりの見出しも出る。
- 確率: 鉄器 0.8% / 中世 各0.5%（計1.5%） / 近代 各0.5% / 現代 各0.5%。浮いたぶんは原木から取った（合計は 10000 のまま）。
- **絵はまだ無い**（`clientmod/Codex依頼文_第6回.md` を Codex へ）。無い間は紙のまま出る。`tokushu_moderu.py` は絵がある物だけ対応表に入れる。
- 知らせは**渡す時**（抽選の時に出すと十連の伏せ札がばれる）。勢力の中には効果まで、全体には名前だけ。

#### 掘りの確率（データパック `loot_tables/blocks/*.json` ＋ プラグイン）

| 石 | 鉄 | ラピス | 金 | ダイヤ | ネザライト |
|---|---:|---:|---:|---:|---:|
| 石・花崗岩・閃緑岩・安山岩 | 15% | **8%** | **4%** | **1%** | 0.1%（プラグイン） |
| **深層岩** | 22.5% | 12% | 6% | 1.5% | **0.2%**（1/500） |

それぞれ独立に判定。**正本は `tests/horu_tsukuru.py` の HYOU**（5つの JSON を作って実機2台へ書く）。`tests/horu_kakunin.py` が JSON と突き合わせる。

### ★★ 2026-08-22: 自己採点（drift-audit）で直したズレ（v0.35.0）★★

`drift-audit` の型で実装⇔文書⇔検証を機械で洗った。見つかって直した物:

| 種類 | 見つかった物 | 直し |
|---|---|---|
| 文書が実装と矛盾 | `README.md`: ビーコン「貯金を0に」/ 生活タブ4品 / ガチャ「はずれ45%」 | 100以下 / 10品 / 新しい割り当てへ |
| 文書が実装と矛盾 | `docs/README.md`: 「傭兵は貴金属を売れません」/ 掘りの期待値 0.37 | 傭兵の店の「売る」へ / 期待値 約0.71円（ネザライト0.1%も明記） |
| 文書が実装と矛盾 | `docs/plugin_README.md`: ガチャ「渡してから演出」 | 「止まった瞬間に渡す」。文書の頭に「2勢力時代の記述」の断り |
| 掃除の漏れ | `reset zenbu` が「開始中」の旗を戻していなかった | 停止中へ戻し、石油も止める |
| 無駄 | `Kanri.seiryoku`（未使用） / `Basho.beaconKuniKazu`（呼び手なし） / `plugin/JidaiCraft-0.28.0.jar`（古い組み立て物） | 消した |
| テストの偽陽性の見分け | ソースを読むだけの検査 53 件に印が無かった | `(字)` を付けた（印つき 62→120 件）。実測と区別して読めるようにした |

残しているズレ（意図的）: `docs/plugin_README.md` は2勢力時代の文書で 川/内海/岩場 が出てこない。
頭の断りで「作りの考え方を読む用」と明記し、正本は本書と `docs/運営コマンドブロック.md`。

#### 確率の正本
- ガチャは `Gacha.KEIHIN`（重み 1万分率）。品ごとの表は `python -c` ではなく GachaTest の出力で読める。
- 掘った時の貴金属は **データパック** `data/minecraft/loot_tables/blocks/stone.json`
  （鉄15% / ラピス5% / 金2% / ダイヤ0.5%。独立判定。石だけ）＋ **プラグイン** `NEZA_KAKURITSU`（ネザライト 1/1000。石と深層岩）。

### ★★ 2026-08-22: 特殊は勢力ごとに各1個・制圧の終戦・拠点のビーコン（v0.34.0）★★

| 指摘 | 直し |
|---|---|
| 特殊アイテムの確率を下げ、**勢力ごとに1個まで** | 重みを半分以下に（鉄器 1.0% / 中世 1.8% / 近代・現代 2.5%）。さらに **勢力ごとに各1種 1個まで**: 出た時点で `config.yml` の `gacha_tokushu.<勢力>` に記録し、以後その勢力には**ふつうの当たりへ引き直す**（`Gacha.kisei`）。同じ十連に2つ出ない。**傭兵・無所属には出ない**。天井の引き直しも特殊を対象にしない。`reset zenbu` で記録も消える |
| 制圧したら花火5発＋終戦の合図 | **制圧 = ビーコンを落とす**。`beaconKowaseruKa` が通ったら、植民地化（データパック）に続けて **戦争を再戦禁止(3)へ進め**（`w_<番号>` タグで該当マーカーだけ命令）、落ちた拠点で花火5発、全員に「終戦」のタイトル＋音＋全体通知。**時間切れの交戦終了には何も足していない**（データパックの1行だけ） |
| 貯金 100 以下で壊せるビーコンを各拠点の後方に | `JidaiCraft.BEACON_KOWASERU = 100`（0 → 100）。`jidai game beacon` が5拠点の**立ち位置の16ブロック後方（-z）**に 鉄3×3＋ビーコン＋勢力色のガラス を置いて登録する（`Basho.beaconNoIchibu` の守る範囲と同じ形）。★ **実機で一度 `/jidai scan` → `jidai game beacon` を打つこと** |
| マルチでしか確認できない検証を別に | **`docs/実機確認リスト.md`** を新設。機械で通った物と、人が見る物を分けた |

#### ★ 設計の注意
- 「制圧」の定義は**ビーコン破壊**に置いた。貯金が尽きて時間切れで付く「占領（senryou）」は従来どおりで、花火は出ない。
- 特殊アイテムの記録は**抽選した時点**で付く。渡すのは後（十連）なので、途中で閉じても二度 出ない。
- ビーコンの「後方」は `BEACON_USHIRO = 16`（立ち位置 = 中心+8、後方 = 中心-8）。拠点の向きを変えたら見直す。

### ★★ 2026-08-22: 傭兵の店・略奪の門番・生活6品（v0.32.0）★★

| 指摘 | 直し |
|---|---|
| **傭兵専用の店（U キー）** | MOD の **U** が `/youheimise` を送り、プラグインが **傭兵の店** を開く（`Shop.YOUHEI`）。**品揃えはご指示で「剣・食料・銃・弓」に確定（v0.33.0）**。防具と建材は無し。値段は**銃と弾が販売所の 2割引き**（`yasuku()`。100未満は丸めない）。**武器と食料の値段は `YOUHEIHIN` に手で書いてある**（矢25・焼肉25＝16.7%引き、パンは 2円のまま3個→4個。2円より下げられないため）。すべて個人の金。タブ4枚 = 武器 / 食料 / 銃 / **売る**（貴金属を売る＝銀行と同じ単価 `Ginko.kikinzokuUru`）。銃と弾は `JUKIHIN` から `youheiJuki()` が作る（ID を二重に持たない）。解禁は中央の時代。**雇われ中は閉まり**、雇い主の拠点の販売所を使う |
| 未雇用の傭兵のリスポーンをランダムに | `jidai game risu youhei`。プラントから **20〜100 ブロック**のでたらめな地面（水・溶岩なら引き直す）。一度 打てば、以後は**死ぬたびに引き直す**（`Kanri.ikikaeru` の頭） |
| 戦争中以外は銀行から略奪できない | `Ginko.osareta` に**戦争の状態の門番**を足した。宣戦なし/再戦禁止 →「戦争していない。まず戦争宣誓を」、準備中 →「あと N 秒」、**交戦中だけ**データパックの略奪へ。★ データパックも同じ判定を持つが、断る理由しか出なかった |
| 原木→板材が作れない | `tsukutteYoi` に `_PLANKS` を足した。板材が無いと作業台が作れず、建材の一覧が全部 飾りだった |
| 銀行を 2段3列に | 枠 3,4,5（預ける 10/50/全部）と 12,13,14（売る/石油/残高）。MOD は段ごとに真ん中へ寄せる |
| 生活欄に6品 | 鉄器: オークの原木×2 10 / 石レンガ×5 12 / 松明×16 5、中世: 焼肉×5 30 / ガラス×5 25、近代: 金リンゴ 100（すべて個人の金）。**生活ページは 12枠の並び（防具と同じ micro）**に組み直した（`mise_kumikae.py`）。枠は 9〜13 と 27〜31 |

#### ★ 傭兵の持ち物の決まり（確認済み・変更なし）

- **時代のツルハシ（壊れない・死んでも落とさない）は傭兵にも初回入室で配られる**（`Tsuruhashi.hajimete` は所属を見ない）。
- **死んだ時、落ちないのはツルハシだけ**。両サーバーとも `keepInventory=false`（level.dat を直接 読んで確認）。他の持ち物は落ちる。
- 傭兵の稼ぎ口は「売る」タブ。銀行の「貴金属を売る」は傭兵には案内だけ出す。
- **MOD の共通画面は、1段目に「▶」付きの品があればタブとして上に描く**（`KyotsuGamen`）。傭兵の店はこれでタブが出る。

#### ★ 作った検査・道具の罠

- **`hyou_tsukuru.py` が UI のカード名と Shop.java の品名を突き合わせる**ようにした。数だけ合って順番がずれると「パンを押したら石炭が来る」店になり、コンパイルも通る。パンと石炭を入れ替えて止まることを確認済み。
- `SeiryokuTest` / `Stage6Test` は枠番号を決め打ちしていたので、生活に品を足しただけで7件 赤くなった。**`wakuOf("戦争宣誓")` のように名前で引く**形へ直した。
- **MOD がサーバーへ物を言う口は `Mc.meirei(cmd)` だけ**（`LocalPlayer#connection#sendCommand`）。独自の通信は作らない決まりのまま。
- 傭兵の店の見出し「傭兵の店」にはパックの絵が無い。`Tsunagu.KYOTSU` は 2列目に「絶対に含まれない字」(U+EDFF) を置いている（空文字だと何にでも当たる）。

#### ★ 設計の注意

- 略奪は **宣戦 → 準備（既定 300 秒）→ 交戦** の順のまま。「宣戦してすぐ」に取らせたければ `settei` の `戦争_準備秒` を縮める（データパック側の設定）。
- 傭兵の店は `Basho` の施設ではない（押すブロックが無い）。`jidai list` には出ない。

### ★★ 2026-08-22: 十連の作り直しと、運営コマンドブロック（v0.31.0）★★

#### ★★★ ここで1つ、道具の罠を書いておく ★★★

**この環境の Bash ツールは、heredoc の中のバックスラッシュを【引用符付き（`<<'PY'`）でも】半分にする。**
`\\n` が `\n` に、`\\"` が `\"` になる。Python のスクリプトを heredoc で渡すと、
逃がし文字を含む置き換えが黙って狂う（KanriTest.java の `\n` が本物の改行に化けた）。
**バックスラッシュを含むスクリプトは、必ずファイルに書いてから `python ファイル` で走らせる。**

#### ガチャのご指摘 6件

| 指摘 | 直し |
|---|---|
| 十連で、結果が出る前にアイテムが付く | **めくり終えてから渡す**形へ。結果を控えておき、①めくり終えた ②途中で閉じた ③切断して次に入った の3つの出口すべてで必ず渡す（`Juren` / `machi` / `watasu10`）。二重渡しは `watashita` で防ぐ。★サーバーそのものが落ちた時だけ残らない（約5秒の窓） |
| 単発をまわすと十連ボタンが居座る | まわしている間は十連ボタンと前回の結果を引っ込め、`modosu()` が戻す |
| 十連の演出を5秒程度で | 伏せ札10枚を **7〜16tick おき**にめくる（頭に溜め、最後の2枚だけ遅く）。合計 **98tick ≒ 4.9秒**。等級で音が変わる（小=拾う音・中=経験値・大=レベルアップ）。大当たりがあれば最後に花火＋全体通知 |
| ガチャ上で特殊アイテムが紙のまま | 2つ直した。①結果画面の `kekkaItem10` だけ CustomModelData が抜けていた ②MOD の古地図画面は **名前で絵を引いて直接貼る**（`Tokushu.java`＝`tools/tokushu_moderu.py` が Shouri.java から生成）。紙の対応表の効き方に左右されない |
| 松明・パンが多い、丸石を +5% | 鉄器: 松明 18→14% / パン 25→21% / 丸石 7→12%。他の時代も同じ向き。金の重みは触っていないので戻りは変わらない。**GachaTest が上限・下限を見張る** |
| 所持金も円表記 | データパック `clock.mcfunction` の actionbar に「 円」を足した（正本と実機の両方） |

#### 運営コマンドブロック（新設 `Kanri.java`・`docs/運営コマンドブロック.md`）

すべて `/jidai game …`。**送り手を問わない**ので、コマンドブロックから打てる。

| コマンド | 中身 |
|---|---|
| `start` / `stop` | 石油の栓の開け閉め ＋ 全員へタイトルと音。★時計は止めない（止めるとサイドバーも止まり「壊れた」と見分けがつかない） |
| `reset` | 中央の時代=1 / 各勢力の時代=1 / 湧き本数=0 / 銃器解放の取り消し / ビーコンとプラントの見た目。**金・戦争・徴収の世代(choshu)には触らない** |
| `reset zenbu` | 上＋ 貯金・個人の金・石油・占領・植民地・下剋上・勝利・戦争マーカー |
| `tp kyoten` | 勢力に所属する全員を拠点へ |
| `tp youhei` | 未雇用の傭兵を、プラントから 90〜110 ブロックの地面へ 1人ずつ別の向きに（水・溶岩の上なら引き直す） |
| `risu` | 勢力の全員の bed spawn を拠点に（force）。以後は `PlayerRespawnEvent` で新しく入った人も拠点へ |
| `spawn [here\|x y z]` | 初期リスポーン。既定 **40 176 -927**（ご指示）。起動のたびにプラグインが世界へ書く |
| `kyoten <勢力>` | 拠点の立ち位置を足元で手動設定（走査の自動値より優先） |

**拠点の位置は座標を書いていない。** `/jidai scan` が施設マーカーの `jidai_kyoten_<勢力>` の印から
「施設の並びの2ブロック手前」を求めて `config.yml` の `kyoten` に保存する。プラントの位置（`plant`）も同じ。
→ **実機で一度 `/jidai scan` を打つこと**（0.31.0 で初めて覚える項目なので）。

★ 世界の名前は `jidai_craft`（フォルダは `world`。Bukkit の World#getName は level.dat の LevelName を返す）。
config.yml の鍵が `jidai_craft,x,y,z` なのはそのため。

#### 検証

- `GachaTest` 124（十連の出口3つ・ボタンの引っ込め・演出の長さ 4〜6秒・重みの上限下限）
- `KanriTest` 17（リセットが触らない物・飛び先 2000回・座標・配線）
- `EnshutsuTest` の音の定数は 7→9 種（`OTO_MEKURI_CHU` / `OTO_TEISHI` を Enshutsu へ）
- わざと壊して落ちることを確認: 十連を「まわした瞬間に渡す」/ 松明を18%に戻す / リセットで choshu を触る

### ★★ 2026-08-22: 特殊アイテムの絵と、すべて古地図化（v0.30.0）★★

#### ★★★ 催しを壊す不具合を1件 見つけて直した ★★★

**ガチャが渡していた特殊アイテムには、名前が付いていなかった。**

```java
// 直す前（Gacha.watasu）
player.getInventory().addItem(new ItemStack(atari.material(), atari.kazu()));
```

勝利判定 `Shouri.motteru` は【紙の表示名】で見分けているのに、渡していたのは
**名前の無いただの紙**。つまり **5種そろえても永久に勝てない**状態だった。
アイテム収集の勝利条件が、まるごと死んでいた。

**なぜ気づけなかったか**: ガチャの検証は「まわして落ちないか」「確率は設計どおりか」
は見ていたが、**渡した物が勝利判定に見つかるか**を誰も見ていなかった。

**直し**: `watasuMono()` を作り、**特殊アイテムだけ**名前と説明を付けて渡す。
（ふつうの景品に名前を付けると、パンがふつうのパンと重ならなくなって持ち物が埋まる）
`GachaTest` に E 章を足し、**昔の形に戻すと落ちる**ことを確認済み。

#### 第5回納品（特殊アイテムの絵5枚）── 受け入れ

`clientmod/nouhin/v5` に展開。**64項目すべて PASS。**

| 時代 | 名前（改名後） | 絵 | 代表色 |
|---|---|---|---|
| 鉄器 | 古びた護符（据え置き） | `gofu.png` | `#8B7250` 土 |
| 中世 | **聖杯の欠片** | `seihai.png` | `#C4934B` 金 |
| 中世 | **羊皮紙の海図** | `kaizu.png` | `#C9AE76` 生成り |
| 近代 | **蒸気機関の歯車** | `haguruma.png` | `#5D482C` 真鍮 |
| 現代 | **月の石** | `tsukinoishi.png` | `#3A4347` 灰 |

**いちばん効いた検査**: 8倍のプレビューが、本番の絵の**最近傍8倍と1画素も違わない**か。
ここが合えば「プレビュー用に別の絵を描いていない」「本番に文字や効果を後から足していない」
が同時に言える。→ 実際に1画素だけ変えて、**4通りの検査が同時に落ちる**ことを確認済み。

#### 絵の入れ方 ── **MOD アイテムを登録していない**

依頼文では「MOD アイテムにする」としていたが、**登録しない形にした。**

```
サーバー: ただの紙 ＋ 名前 ＋ CustomModelData(8201〜8205)
MOD 側 : assets/minecraft/models/item/paper.json を上書きし、
         「この番号の紙は この絵で描く」という対応表を持ち込む
```

| なぜこちらか | |
|---|---|
| サーバーに MOD を入れなくてよい | Arclight に Forge アイテムを登録させ、それを Bukkit の ItemStack として扱う経路は、実機で試す手間と壊れ方が読めない |
| 既存の仕組みをそのまま使える | 勝利判定は「名前の付いた紙」を探す。**1行も変えなくていい** |
| MOD 無しの人も困らない | 紙のまま見えるが、名前で何かは分かる |

★ Forge の mod の assets は vanilla より後に読まれる（＝勝つ）。
実機のクラッシュ報告の `Resource Packs: vanilla, tacz_resources, mod_resources` が根拠。

★ **番号と絵の対応表は `Shouri.java` が正本**。`tools/tokushu_moderu.py` がそこから
paper.json を作る。**手で写さない。**食い違うと「別の絵が出る」のに
サーバーは正しく動くので実機で見るまで気づけない
→ GachaTest と mod_kakunin の両方が突き合わせる（わざとずらして落ちることを確認済み）。

#### すべて古地図化（ご指示）

| 画面 | 描き方 |
|---|---|
| 販売所・銃器専門店 | Codex の下地の絵（`UiGamen`）※従来どおり |
| 十連の結果 | 線と塗りだけ（`JurenGamen`） |
| **ガチャ・銀行・時代を進める・宣戦** | **`KyotsuGamen`（共通）** |

**素のチェスト画面のまま残っている物は もう無い。**

`KyotsuGamen` は画面ごとに作っていない。**中身を読んで、そのまま並べる。**
この4つは「名前と説明の付いたアイテムが数個ならぶ。押すと何かが起きる」で作りが同じなので、
**プラグイン側で枠を足しても MOD は何も直さずに追随する**。

- 値段・条件・残高は、プラグインが**アイテムの説明文**に書いている。
  板に並べる余地が無いので、**カーソルを乗せた時だけ吹き出し**で出す。
- ガチャだけ、1段目が3枠以上 埋まっていたら**回転帯**として描く（真ん中を金の枠で指す）。
- 未開放の枠は販売所と同じく **「？？」**。
- 色・板・枠・ボタンは **`Kazari.java` に集約**。画面が増えても、直し忘れた画面だけ浮くことがない。

★ `Tsunagu` は**日本語の見出しと、パックの絵の文字の両方**を見る。
`jidai ui on` にすると見出しが絵の文字だけになり、名前だけで見分けていると
**どの画面も古地図に化けなくなる**（既定は off なので今は日本語）。

#### はみ出しを、実機を見ずに潰す

掲示板で2度 やり直した「はみ出し」は、**ただの算数**だった。
`mod_kakunin.py` が KyotsuGamen.java から定数を読んで、
**論理幅 320（自動倍率のいちばん狭い形）と 480（実機）で溢れないか計算する。**

```
自動倍率のいちばん狭い画面: 板 295x122（ひと枠 31）
実機（1920x1009 / 倍率4）: 板 448x122（ひと枠 48）
```

→ 実際、最初の書き方は 320 で **板 412 > 画面 320** と溢れていた。
（板だけ画面幅に丸めていたので、枠が板からはみ出す形だった）
わざと元に戻して、この検査が落ちることを確認済み。

### ★★ 2026-08-22: クラッシュの真因と、ガチャの作り直し（v0.29.0）★★

#### ★★ クラッシュ `NoClassDefFoundError: SokuteiGamen$Kekka` の真因 ★★

**遊んでいる最中に jar を差し替えたから。**（15:57:17 に差し替え → 15:58:30 に落ちた）

Forge は起動時に jar の目次（どの class がどこにあるか）を覚える。
中身を入れ替えると目次が実物とずれるが、**既に読んだ class は平気**なので、
差し替えた直後は何ごともなく動いて見える。
**まだ一度も読んでいない class** を読む場面に入って、そこで初めて落ちる。
これが「入れ替えてしばらく遊んでから、急に落ちる」の正体。

**対策2つ:**

1. **`haichi.py` が Minecraft の起動中は入れない**ようにした
   （`Get-CimInstance Win32_Process` で javaw/java を見る。`--muri` で押し切れる）。
2. **`mod_kakunin.py` が「組んだ class が1本残らず jar に入っているか」を見る**。
   詰め残しでも まったく同じ落ち方をし、しかも**その class を初めて読むまで
   気づけない**。入れ子の class（`$` つき）はうっかり弾きやすい。
   → わざと1本 余らせて落ちることを確認済み。

**あわせて、開発用のキーを `Ctrl+Shift+J` / `Ctrl+Shift+K` に変えた。**
素の J / K のままだと、50人が遊んでいる最中に誰かが押して知らない画面が出る。
（今回のクラッシュも、K で開く測定画面がきっかけだった）

#### ガチャの直し 3件（2026-08-22 のご指摘）

| 指摘 | 原因と直し |
|---|---|
| 一度まわすと、UI を出るまで もう一度まわせない | **止まった景品が「まわす」ボタンの枠(4)に居座っていた。** 枠を押せば実はまわせたが、ボタンが消えて景品の名前になるので**まわせるように見えなかった**。→ 1.5秒 見せてから **必ずボタンへ戻す**（`modosu`）。流れ終わった帯も片づけ、直前の結果は枠9へ移す。★ まわしている最中なら戻さない（次の演出を壊すため） |
| まだ かなり渋い | 渋さの正体は はずれ率より **金の戻りが値段の2〜3割しかない**ことだった。→ **はずれ45%→20% / 金の戻り→約6割 / 大当たりの見出し 13〜15%→23〜29%** |
| 十連ガチャが欲しい（UI も） | **十連＋天井**を作った。10回のうち必ず1つは「中」以上。結果は **27枠の別画面**で、MOD が古地図の顔に描き直す（`JurenGamen.java`） |

**★ 金の戻りを6割に留めてある理由**
ここを 100% に近づけると、ガチャが【金を増やす手段】になり、
売って稼ぐ・時代を進める、という筋がまるごと壊れる。
**GachaTest が毎回 期待値を計算して「値段を超えない」ことを見張る**
（わざと超えさせて落ちることを確認済み）。

| 時代 | 値段 | はずれ | ふつう | 金 | 特殊 | 特賞 | 金の戻り | 大当たり |
|---|---|---|---|---|---|---|---|---|
| 鉄器 | 5 | 20.0% | 57.0% | 20.7% | 2.3% | 0.2% | 51.0% | 23.0% |
| 中世 | 10 | 20.0% | 53.2% | 23.0% | 3.8% | 0.2% | 62.5% | 26.8% |
| 近代 | 20 | 20.0% | 51.5% | 22.7% | 5.8% | 0.2% | 61.3% | 28.5% |
| 現代 | 50 | 20.0% | 51.5% | 22.7% | 5.8% | 0.2% | 61.8% | 28.5% |

**天井が仕事をしている証拠**: 天井が無ければ、十連の
**43%（鉄器）／22〜25%（中世以降）** が「小」以下だけになる。

#### 十連の作り ── 新しい通信は1つも足していない

```
プラグイン: 27枠のチェストを「十連の結果」という見出しで開く
            枠 {2,3,4,5,6, 11,12,13,14,15} に結果10個
            枠 21 = もう一度 / 枠 23 = 閉じる
            名前の頭に等級の印（大=★ / 中=◆）を付ける
   MOD 側 : 見出しで見分けて JurenGamen に差し替え、上の枠を読んで描く
            印を見て色を決める（★=金 / ◆=水色）
```

- **MOD を入れていない人**は素のチェストで同じ10個が並ぶ。印も見えるので等級が伝わる。
- **★ 枠と見出しと印は、プラグインと MOD の両方に書いてある。**
  食い違うと **MOD 側だけ空**になり、しかもプラグインは正しく動くので実機で見るまで
  気づけない。→ **GachaTest が両方のソースを突き合わせる**（わざとずらして落ちることを確認済み）。

#### 検証側で見つけて直した穴 3つ

1. **`run_harness.py` の文字化け** ── javac は cp932、java は UTF-8 で日本語を出す。
   片方に決め打ちすると、もう片方が必ず化ける。化けたせいで
   「runTaskLaterの参照はあいまいです」という肝心のエラーが読めなかった。
   → **通った方の文字コードを採る**（`yomu()`）。
2. **検証が景品表を写し取っていた** ── `Stage7Test` / `EnshutsuTest` に
   `{"パン×2", ...}` `{0, 95}` と期待値を手で書いてあり、表を入れ替えたら
   **中身は正しいのにテストだけ古くなって8件 赤くなった**。
   → **期待値を KEIHIN から導く**形へ書き換えた。
3. **`GachaTest` を新設**（64項目）。表の割り当て・金の期待値・天井・
   ボタン復帰・MOD との突き合わせを見る。

### ★★ 2026-08-22: かまどの真犯人と、特殊アイテムの改名計画 ★★

#### ★★ 「直したのに直っていない」の正体（2026-08-22）★★

**2回とも、実機の mods に古い jar が残っていただけ**だった。
原因は**案内していた cp のパスが間違っていた**こと。

| | |
|---|---|
| 遊んでいる場所 | **`Desktop/実機演習 1.20.1/mods`** |
| 間違って案内していた場所 | `%APPDATA%/.minecraft/mods`（1.5.2 の古い MOD が入ったまま。**ここではない**） |

**`clientmod/haichi.py` を作った。** TaCZ が入っているかで「本当に遊んでいる mods か」を
見分けて入れ、置いたあと **sha256 で組んだ物と一致するかを照合**する。
**以後は手で cp しないこと。**「入れたつもり」を残さないための道具。

#### 実機の指摘 第3波（MOD 側・2026-08-22）

| 指摘 | 原因と直し |
|---|---|
| 掲示板の上からバニラのサイドバーが覗く | 被せて隠す形をやめ、**`VanillaGuiOverlay.SCOREBOARD` を取り消して そもそも描かせない**形にした。バニラは「行の下端 = 画面の半分 + 行数×3」という独特の置き方で、大きさを合わせにいく限り必ずどこかで はみ出す |
| 未開放の品名が見える | MOD は設計の文字をそのまま描いていた。**未開放の枠は品名を「？？」・値段は非表示**（解禁時代だけ残す）に |
| 勢力の金が 0 のまま | 0.27.0 の枠8が 0 を書いていたため。**サイドバーの「貯金」行を第一に読む**形へ（枠8は控え） |

#### かまどの炎が出ない ── 真犯人が確定（v0.28.0 に修正入り）

**警告ログが出ないこと**が決め手だった。`hyoujiKousin` は
`view.getTopInventory() != h.inv` で「同じ画面か」を見てから送っていたが、
**Arclight は getTopInventory() のたびに新しい包みを返すことがある**ため、
同じ中身でも毎回 false → **setProperty の手前で毎回 引き返していた**
（焼く処理は正常。燃料だけ減って炎が出ない、という実機の症状と一致）。

**「h.inv を見ている人の一覧に居るか」（`getViewers().contains(p)`）へ変えた。**
KamadoTest が「== 比較に戻っていないか」をソースで見張る（708 PASS）。
★ **実機で直っていることを確認済み**（ユーザー確認・2026-08-22）。

#### 特殊アイテム5種 → MOD の専用アイテムへ（計画）

**★ 完了（2026-08-22）。** 絵は納品・受け入れ済み、改名も反映済み。
以下は経緯。名前の正本は `Shouri.TOKUSHU_HYOU`。

| 旧名 | → 新名 | 時代 |
|---|---|---|
| 古びた護符 | 古びた護符（そのまま） | 鉄器 |
| 錆びた鍵 | **聖杯の欠片** | 中世 |
| 色あせた地図 | **羊皮紙の海図** | 中世 |
| 欠けた歯車 | **蒸気機関の歯車** | 近代 |
| 黒い手帳 | **月の石** | 現代 |

> **★★ 改名は「MOD アイテム化」と同時に、3か所いっぺんに ★★**
> `Shouri.TOKUSHU`（正本）・`Gacha.KEIHIN`・`UneiTest` の突き合わせ。
> 片方だけ変えると【永久に集まらない勝利条件】になる。
> 金床の封鎖は、MOD アイテム化（表示名でなく物そのもので判定）が済めば
> 理屈の上では不要になるが、外すかどうかは別途 判断。

### ★★ 2026-08-22: 実機の指摘 第2波（v0.28.0・差し替え待ち）★★

| # | 指摘 | 直し |
|---|---|---|
| 1 | **勢力の金が 0円** | 下の「金庫の対応表」の既存バグ＋新規2か所。**直した** |
| 2 | 未開放の名前 | 「??? 未開放」→「**？？**」 |
| 3 | **E キーで退出** | `Options.keyInventory` に聞く形で対応（キーを替えている人にも従う） |
| 4 | サイドバーに 円・ℓ・時代名 | **バニラの数字列には文字を足せない**（仕組み上 不可能）。**MOD の掲示板で対応**（下記） |

#### ★★ 金庫の対応表の既存バグ（今回いちばん重い発見）★★

`Kane.KINKO`（チーム名→貯金の持ち主名）が **2勢力ぶんしか無かった**。
5勢力に増やした時に取り残され、**川・内海・岩場は 銀行・宣戦・勢力の金での
買い物が まるごと使えなかった**（kinkoMei が null）。
コンパイルも当時の検査も通るので、人が試すまで分からない形の事故。

- **対応表そのものを消し、正本を `Seiryoku` ひとつに**した（`Kane.kinkoMeiTeam`）
- 新規バグ2か所も同根: 枠8の残高と銃器の解放が `teamMei`（チーム名）で
  貯金を引いていた。**貯金の持ち主は日本語名**（丘陵・森林…）
- `UneiTest` が **5勢力ぶんの対応と、データパック側（kinko_yomu.mcfunction）
  との一致**を毎回 突き合わせる（+12件）

#### MOD の掲示板（`clientmod/src/main/java/jidai/ui/Keiji.java`）

サイドバーを古地図の顔で描き直す。**中身はサーバーのサイドバーそのもの**
（hyouji_N はクライアントへ届いている。読んで描き直すだけ）。

- **貯金 5,969円 / 石油 12ℓ / 時代 鉄器・中世・近代・現代** の表記
- バニラのサイドバーは消せない（イベントが無い）ので、
  **同じ場所に不透明で被せて隠す**。被せ幅はバニラの描く幅と比べて大きい方
- 行を足す・減らすのは今までどおりデータパック側の仕事

#### ★ run_harness の穴（ついでに塞いだ）

ハーネスのコンパイル失敗を **stderr の "error" という英単語**で見ていたが、
この環境の javac は**日本語で「エラー」と出す**ため、壊れていても素通りして
【全体が緑のまま】になっていた（UneiTest が0件なのに 0 FAIL と出た。実際に起きた）。
**返り値で判定する形に直し、わざと壊して exit=1 になることを確認済み。**

### ★ 2026-08-22: 銃の見た目と、銃器専門店の解放（v0.27.0）

#### 銃と弾のアイコンが本物になった

**サーバー（Bukkit）は MOD のアイテムを作れません**（`Material` はバニラ決め打ち）。
そのためチェスト画面にはクロスボウ等の**代用品**しか置けませんでした。
**クライアントには TaCZ が入っている**ので、MOD 側で組み立てて描くようにしました。

- 正本は `Shop.java` の `modItem` 欄。生成器がそこから拾うので、
  **MOD 側に銃の名前は1つも書いていません**
- NBT は `TagParser` で付ける（`tacz:modern_kinetic_gun{GunId:"hamster:sks"}`）
- 銃9丁・弾6種の**12件すべて**に指定が入った

**絵を選ぶ順**は次のとおり。上から先に決まったものを使います。

1. **まだ買えない** → サーバーが置いた灰色の板ガラス（**状態と絵を必ず一致させる**）
2. **MOD のアイテム** → 本物の銃・弾
3. サーバーが置いた物
4. 代用品（**TaCZ が入っていない人でも絵が出る**）

> **★ TaCZ が実際にその見た目を描くかは、実機でしか分かりません。**
> TaCZ は独自の3D描画を使うので、こちらで組み立てた
> （弾数などの入っていない）アイテムをどう描くかは動かすまで不明です。
> **描けなければ自動で代用品に落ちる**ので、悪くなることはありません。

#### 銃器専門店の解放（ご指示）

**中世に入っただけでは銃を買えません。**
**勢力の貯金が一度でも 10,000 を超えたら、その勢力は恒久的に開きます。**

| | |
|---|---|
| 判定 | `Shop.JUKI_KAIHOU = 10000`。**「超えた時」なので、ちょうど 10,000 では開きません** |
| 記録 | プラグインの config（`juki_kaihou.<勢力>`）。**再起動しても消えません** |
| 見張り | 1秒ごと。開いた瞬間に**その勢力の全員へ通知**＋音 |
| 開いたあと | 今までどおり**時代ごとに順に解禁**（＝「連鎖」） |

> **★ 「今の残高が 10,000 以上の間だけ」にはしませんでした。**
> 12,000 の M107 を買った瞬間に店が閉じ、
> 「買えたのに買えなくなる」という説明のつかない挙動になるためです。

> **★★ 門は2か所にあります ★★**
> 画面を作る所だけだと、開いたまま放置した古い並びから押せてしまう。
> 買う手続きだけだと、買えない品が買えるように見えてしまう。
> **`BukiTest` が両方あることを見張っています**（わざと外して落ちることを確認済み）。

### ★ 2026-08-22: 武器タブに 弓・矢・クロスボウ（v0.26.0）

| 品 | 値段 | 財布 | 解禁 |
|---|---|---|---|
| 弓 | 150 | 勢力 | 鉄器 |
| 矢 ×10 | 30 | **個人** | 鉄器 |
| クロスボウ | 150 | 勢力 | 中世 |

**武器タブは 2枠 → 5枠**（鉄の剣・弓・矢・ダイヤの剣・クロスボウ）。並びは時代順です。

> **★ 矢だけ個人の金。**撃つたびに減る消耗品なので、勢力の貯金から出すと
> 時代進行が止まります（銃の弾と同じ考え方）。
>
> **★ 範囲外の気付き**: 銃器専門店は、コルト M1851/M1873 の見た目に
> **クロスボウの絵**を代用として使っています。武器タブに本物のクロスボウが
> 並んだので、**絵だけ見ると紛らわしい**かもしれません。
> 気になるなら銃側の代用アイコンを別の物に替えられます（未対応）。

### ★★ 2026-08-22: 実機の指摘4点（v0.25.0）★★

| # | 指摘 | 原因 | 直したか |
|---|---|---|---|
| 1 | タブを切り替えても見出しが「生活」のまま | **開いた瞬間に1回だけタブを判定していた。** サーバーは「画面を開け」と「中身」を別々に送るので、判定時はまだタブの印（`▶`）が届いていない | **直した**（毎フレーム 判定し直す） |
| 2 | 勢力の金が「?」 | `chokin` はサイドバーに**文字の行**としてしか出ておらず、クライアントへ届かない | **直した**（プラグインが枠8に金額を書いた品を置く） |
| 3 | 剣の「武器」タブが欲しい | ── | **作った**（販売所は3タブに） |
| 4 | エンダーかまどで炎が点かない | **焼く処理は正常。画面へ状態を送る所が届いていない疑い** | **切り分け中**（下記） |

#### ★ かまどの切り分け（#4）

**`Kamado` には検証が1件も無かった**ので、まず書きました（`KamadoTest` 15項目）。

**焼く処理は正しく動いています。**

- 材料が無ければ燃料は減らない
- 材料があれば燃料が1つ減り、火が点く
- **120tick（鉄器）で焼き上がり、材料が1つ減る**
- 出来上がりに別の品があれば火を点けない

ご指摘の症状「**燃料は消えるが炎が出ない**」とも一致します。
つまり残る疑いは **`InventoryView.setProperty` が画面へ届いていない**こと。
CraftBukkit は、独自に作った FURNACE の入れ物を汎用の容器で開くため、
炎の値を送る口が働いていない可能性があります。

**確かめる仕掛けを入れました。** 送れなかったら `latest.log` に1回だけ出ます。

```
エンダーかまど: 炎の状態を画面へ送れませんでした（setProperty が false）。
```

- **この行が出たら** → `setProperty` が犯人。かまどを「本物のかまどブロック」で開くか、
  9枠のチェスト画面へ作り替えて進み具合を自前の絵で見せる、のどちらかになります
- **出ないのに炎が出ないなら** → 値は届いているのに絵へ反映されていないので、別の原因です

#### ★ ついでに直した実測のバグ

**石のハーフが燃料になっていました。** `_SLAB` で終わる名前を全部 燃料（300tick）に
していたためです。**同じ名前の板があるかどうか**で木かを見分ける形に変えました
（新しい木が増えても勝手に付いてきて、石化した樫のハーフのような
「木の名前だが燃えない物」も正しく外れます）。木のハーフは 150tick です。

### ★ 2026-08-22: ガチャを3点 直しました（v0.23.0）

ご指摘は「表示が二重」「何が出ても大当たり」「当たりが少なすぎる」の3点。

| # | 直したこと | 中身 |
|---|---|---|
| 1 | **「パン 32 ×32」の二重表示** | 景品の**名前から個数を外した**。表示は `Gacha.hyouji()` が「パン ×32」に組み立てる |
| 2 | **何が出ても見出しが「大当たり」** | 見出しは **お金と特殊アイテムだけ**（`Gacha.oomonoKa()`）。それ以外は新しい種別 `Enshutsu.GACHA`（灰色●）|
| 3 | **当たりが少なすぎる** | **はずれ 70% → 45%** |

**新しい割り当て**（時代ごとに重みの合計はきっちり 10000）

| | 鉄器 | 中世 | 近代 | 現代 |
|---|---|---|---|---|
| はずれ | 45% | 45% | 45% | 45% |
| ふつうの当たり | 41.5% | 40.5% | 40.5% | 40.5% |
| お金 | 12.5% | 11.5% | 11.5% | 11.5% |
| 特殊アイテム | 1.0% | 3.0% | 3.0% | 3.0% |
| **大当たりの見出しが出る率** | **13.5%** | **14.5%** | **14.5%** | **14.5%** |
| 金の期待値（値段比） | 22% | 29% | 29% | 22% |

> **★ 全体チャットと花火は、いちばん上の等級(DAI)だけのままです。**
> 個人への見出しだけを出し分けました。広げると通知が増えすぎて価値が下がります。
>
> **★ 特殊アイテムの名前は `Shouri.tokushuMei()` を正本にしています。**
> Gacha 側で並べ直すと、片方だけ変えた時に「永久に集まらない勝利条件」になります。

**枠番号を直書きしていたテストが落ちました**（`EnshutsuTest` がソースの
`t.equals(DAI)` という**書き方**を見ていた）。判定の意味は変えずに式の形へ追随させ、
新しい出し分けの検査を2件 足しました（653 → 655）。

### ★ 2026-08-22: 剣を2本 足しました

**鉄の剣 100 / ダイヤの剣 500（どちらも勢力の金・鉄器/中世で解禁）。**

| 変わったところ | |
|---|---|
| プラグイン | `Shop.java` の `IPPAN`。**タブ1の枠が 9〜14 に詰まった**（戦争宣誓 12→13 / 下剋上 13→14） |
| 枠番号を直書きしていたテスト | `SeiryokuTest`(5か所) `Stage4Test` `Stage6Test` を直した |
| UI | 生活タブを **142×76 の4枠 → 94×52 の6枠**（銃器専門店と同じ並び）へ組み替え |
| 絵 | **1枚も作っていない。** 第4回で納品・検査済みの部品を並べ直しただけ |

> **★ 組み替えは `clientmod/tools/shop_life_kumikae.py` が正本です。**
> 手で `manifest.json` を書き換えていません。品を増減する時はこの道具を直します。
> `manifest.json` の `provenance.modified_after_delivery` に、
> 納品後に手を入れたことを記録してあります。

> **★ 2026-08-21: 第3回の納品が入り、32件すべて通りました。**
> ①8枚そろった（`shop2` `juki2` あり）②高さが正しい（販売所 222 / 銃器専門店 186）
> ③持ち物欄が 100% 塗られている ── 3点とも解消しています。
>
> **ただし「機械で通った」＝「配ってよい」ではありません。**
> `ascent`（絵の上下位置）だけは実機で開いて目で見るまで分かりません（§4 の #10）。

> **Gradle はこの環境では動きません**（Java の NIO Selector が塞がれている）。
> `.jar` は `javac` + `zipfile` で組みます。スキル `mc-bukkit-plugin` の
> `scripts/build_jar.py` がそれ。

---

## 6. 残っている仕事（優先順）

### A. UI ── ★★ 2026-08-21、方式を変えました ★★

**チェスト型 GUI（リソースパック）を取りやめ、クライアント側の Forge MOD へ移しました。**

やめた理由は絵の質ではありません。**方式の上限**です。

| チェスト型の固定値 | 起きたこと |
|---|---|
| 横幅 **176px 固定** | 「所持金」の欄すら右に置けない |
| 枠が **18px 刻み** | 「チェーンの胸当て:勢力 25」が入らない |
| 文字は**絵に焼くしかない** | 日本語が 7px まで縮み、漢字がつぶれた |
| 変わる数字が**出せない** | 所持金・残高を画面下の帯へ逃がしていた |

#### 実証したこと（2026-08-21）

| | |
|---|---|
| **Gradle 無しで Forge MOD が組めるか** | **組める。**`clientmod/build.py`（javac + zipfile） |
| なぜ組めるか | 本番の Forge は **SRG 名**で動く。ForgeGradle は「本来の名前→SRG へ変換」の二段構えだが、**最初から SRG 名で書けば変換が要らない** |
| 読めるコードにできるか | **できる。**SRG 名は `Mc.java` の1ファイルに閉じ込め、他は日本語の名前だけ |
| SRG 名の対応表 | `clientmod/tools/namae.py`。Mojang 公式 `client.txt` と Forge 同梱表を突き合わせ、**メソッド 59,178 / フィールド 33,135** |
| **実機で画面が出るか** | **出た。**日本語がつぶれない・名前と価格がカードの中・ホバーで枠が光る、を目視で確認 |

> **★ `client.txt` は Mojang のライセンスで再配布 禁止**です。MOD には同梱しないこと。

#### 試作の既知の不具合

**`ShisakuGamen` は 470×286 で決め打ちのため、GUI スケール4（論理 478×252）で上下が切れます。**
本番は **320×240 を基準**に作ること（Minecraft の自動スケールはここを下回らない。`Window` の
即値で確認済み）。画面が広い時は最大2倍まで拡大する作りにする。

#### 第4回の納品（2026-08-22）── **受け入れ済み**

`clientmod/ui/` に取り込みました。受け入れ時は **117 PASS / 0 FAIL**（`tests/sozai_kakunin.py`）。
（その後 武器タブのページを足したので、**いまは 129 項目**です）

| | |
|---|---|
| 中身 | PNG 47枚 + `DESIGN_SPEC.md`（589行） + `manifest.json` |
| 画面 | 8ページすべて **320×240 に収まり、スクロール不要** |
| 素材 | 9分割3種 / カード5寸法×5状態 / タブ / ガチャ・銀行・宣戦の専用部品 |
| 色 | **最大10色**（上限256）。半透明ゼロ |
| **文字** | **焼かれていない**（下記の検査で実物から証明） |

**★ いちばん効いた検査**: 「合成図（`page_*.png`）＝部品を並べたものと画素まで一致するか」。
一致すれば **①設計書の座標が実物と合っている ②余計な焼き込みが無い** が同時に言えます。
加えて manifest が申告する「文字を置く領域」が**本当に無地か**を画素で確認しています。
**わざと壊して2種類とも落ちることを確認済み**（カードを1pxずらす / 文字領域に1画素描き込む）。
1画素の混入は**4つの別々の検査**に引っかかります。

#### 描画まで実装済み（2026-08-22）

| | |
|---|---|
| **文字が枠に収まるか** | **実機で測って 全135件・あふれ0件**（`K` キーの測定画面） |
| 描画 | 8画面すべて。`J` キーで開き、左右の矢印で切り替え |
| 素材 | 絵47枚を jar に同梱（`assets/jidaiui/`） |
| 検査 | `tests/mod_kakunin.py` **48 PASS**（SRG 名を全件 逆引きで検算） |

**★ 座標を手で書いていません。** `ui/manifest.json` から `tools/hyou_tsukuru.py` が
`Hyou.java`（文字135・カード42・画面8）を生成します。**`Hyou.java` は手で直さないこと。**
直すなら manifest か生成器を直して、作り直します。

**★ 折り返しは Minecraft 本体（`Font#split`）に任せています。**
自分で書くと本体と1文字ずれただけで「測った時は収まっていたのに、実際ははみ出す」が起きます。

**★ 拡大は整数倍だけ。** 1.5倍だとドット絵の線がまだらになります。
設計が 320×240 なので、自動スケールの下限（320×240）でも必ず1倍で収まります。

#### アイテムの絵まで実装済み（2026-08-22）

**カードにアイテムの絵が出るようになりました（33件）。**

**★ どの枠に何を置くかは `plugin/src/main/java/jidai/Shop.java` が正本です。**
`tools/hyou_tsukuru.py` が Shop.java を読んで `Hyou.KADO` に焼き込みます。
MOD 側でアイテム名を書いていません（書くと「絵と中身が違う店」になります）。

> **★★ 数が合わないと、生成器がその場で止まります ★★**
> 「UI のカードは6枚なのに Shop.java は5品」のような食い違いを検出します。
> **両方向とも わざと壊して確認済み**（UI のカードを減らす／プラグインの商品を消す）。

**★ アイテムの決まっていないカードが11枚あります。**
銀行6（10預ける・50預ける・全部預ける・貴金属を売る・石油を預ける・残高）、
時代を進める1（申請）、宣戦4（相手の勢力）。
**何を置くかは決まっていません。**宣戦は勢力の色が要るので、
通信ができてからのほうが自然です。

#### ★★ 通信まで実装済み ── 実際に買えます（2026-08-22）★★

**サーバーを1行も変えていません。**

| やり方の候補 | 採否 |
|---|---|
| 独自の通信を作る（プラグイン側も改造して値段や残高を送る） | **採らなかった** |
| **今までどおりチェスト画面を開かせ、その上に絵を被せる** | **採用** |

**なぜ被せる方を採ったか（3つ）**

1. **サーバーを変えずに済む。** 買う処理は 655項目で確かめた既存のものがそのまま動く
2. **MOD を入れていない人は、今までどおりチェスト画面で買える**（保険が要らない）
3. **解禁や在庫の判定を二重に持たなくてよい。**
   プラグインは未解禁の枠を灰色の板ガラスで塞ぐので、**中身を見るだけ**で状態が分かる

**仕組み**（`clientmod/src/main/java/jidai/ui/Tsunagu.java`）

- `ScreenEvent.Opening` で、開こうとしている画面の**見出し**を見る
- 「販売所」「銃器専門店」なら、こちらの `UiGamen` に**差し替える**
- どのタブかは、プラグインが選択中のタブ名の頭に付けている**「▶」**で見分ける
- カードを押したら、**その枠を左クリックしたことにして送る**
  （枠番号は `Shop.java` から生成した `Hyou.KADO.waku`）
- タブを押したら枠0・枠1を送る → プラグインが開き直してページが変わる
- Esc で `Player#closeContainer()` を送る（**送らないとサーバー側は開いたままだと思い続ける**）

**★ 所持金は本物が出ます。** `kane_kojin` は Tab の一覧に出ているので、
クライアントのスコアボードから読めます（`docs/SCOREBOARD.md` の記述どおり）。

> **★ 勢力の金は読めません。**サイドバーに**文字の行として**出ているだけで、
> 目的そのものがクライアントへ来ないためです。**「勢力 ?」と出します。**
> 分からない物に数字を出すと嘘になるので、そうしています。
> 出したいなら、プラグイン側で `chokin` を何かの表示スロットへ出すのがいちばん安い。

#### まだできていないこと

| | |
|---|---|
| ガチャ・銀行・時代を進める・宣戦 | **被せていません。**素のチェスト画面のままです |
| 持ち物欄 | **見えません。**設計に持ち物の欄がないため（買うのに支障はない） |
| 勢力の金 | 上記のとおり「?」 |
| 銀行・時代を進める のアイコン | 何を置くか**未定**（ユーザーの判断） |

#### 次にやること

1. **実機で買えるか確かめる**（下の §D に足しました）
2. ガチャ・銀行・時代を進める・宣戦にも被せる
   ★ ガチャは「まわす」が枠4。銀行は枠2/3/4/6/7。**カードの枠番号を決める必要がある**
3. 勢力の金を出すかどうか（出すならプラグイン側に1行）
4. 参加者向けに MOD を配る手順を `jikki_mod/クライアント用/` へ足す
5. `UiGamen.annai()`（下の操作説明）は**本番では外す**

#### ★ 予定に入れたこと（ユーザーの意向・2026-08-22）

**時代ごとの特殊アイテム5種を、MOD 側の本物のアイテムとして作る。**

いまは `Material.PAPER` に表示名を付けたもので代用しています。
**表示名で本人確認しているので、金床を塞いで改名を防いでいます**（`Shouri`）。
MOD で専用アイテムにすれば、その回り道が要らなくなります。

> ★ 移す時に注意: 名前は `Shouri.tokushuMei()` が正本で、
> ガチャの景品表・勝利条件・`UneiTest` の3か所が突き合わせています。
> **アイテムを差し替えるなら、この突き合わせも一緒に作り直すこと。**

> **★ 見た目は `Navigation`（航海・測量）案で決定しました。**残り3案は使いません。

---

### A-2. リソースパック（第3回）── ★ **配らないことに決めました（2026-08-21）**

**2026-08-21: 第3回の納品が届き、`resourcepack/codex/` へ反映しました。**
第2回は上書きしましたが、
`Documents/Codex/2026-08-20/files-mentioned-by-the-user-codex/outputs/JidaiCraft_UI_Delivery.zip`
から戻せます（第2回にしか無かった `IMAGEGEN_PROMPTS.md` と `source_materials` もその中）。

機械で見た結果:

| 検査 | 結果 |
|---|---|
| `python tests/ui_kakunin.py` | **32 PASS / 0 FAIL** |
| スキル `mc-bukkit-plugin` の `pack_check.py` | **49 PASS / 0 FAIL**（色数 249〜254 / 持ち物欄 100%塗り） |

第3回で頼んだ5点は**5点とも入りました**（8画面 / 全面塗り / 高さ / タブ / AIっぽくない）。

> **★★ 監査で3つ見つかりました。どれも判断が要ります ★★**
>
> **① `container.inventory` を空文字で上書きしている**（4案とも・第2回には無かった）
> 焼き込んだ台帳とバニラの「持ち物」の字が重なるのを避けるため入れたもの。
> **パックを入れているあいだ、チェスト・かまど・MOD の画面からも「持ち物」の字が消えます。**
> `jidai ui off` では戻りません（あれはプラグイン側の文字だけを切り替えるため）。
> 消したくなければ、4案の `assets/minecraft/lang/` を消せば済みます
> （納品元も `IMPLEMENTATION.md` で「外して構わない」と書いています）。
>
> **② 名前と価格が、対応するアイテムの真下に無い**
> 銃器専門店は9丁が1段に並ぶのに、名前は3列×3行の一覧。
> 販売所タブ2の「鉄の防具」は、品物が2段目なのに説明は6段目。
> **これは指示書（第3回 §5）の前提が間違っていたためです。**
> 枠1つは18pxしかなく、「チェーンの胸当て:勢力 25」は日本語では入りません。
> 直すなら、指示書のほうを「一覧として読める形」に書き換えるのが筋です。
>
> **③ 台帳の文字が 7px で、漢字がつぶれ気味**
> 見出しは 7〜8px → **13〜14px** になり、明らかに読めるようになりました。
> つぶれているのは**台帳のほう**です。とくに販売所タブ2のダイヤ行は
> 字の色が背景に近く、読みにくい。**実機で見てから決めてください。**

> **★ 配らないことに決めました（2026-08-21・ユーザー判断）。**
> 理由: パックがゲームに与える影響は3つ（8画面の絵 / フォントのグリフ /
> `container.inventory` の空文字上書き）で、**MOD へ移ると3つとも要らなくなる**。
> しかも絵は既定 `off` で出ないのに、**上書きだけは切れない**ため、
> いま配ると「得が無いのに副作用だけ受ける」状態になります。
>
> **やったこと**: `README.md` と `jikki_mod/手順.md` のパック配布の節を差し替え、
> `jikki_mod/クライアント用/resourcepacks/` の案内を
> 「このフォルダは使いません.txt」に書き換えました。
> **プラグインのコードは1行も触っていません**（`jidai ui` は既定 off のまま眠ります）。
>
> **★ 案は `Navigation`（航海・測量）で決定。**採用先は **MOD の見た目**です。
> **`SCOREBOARD_TEXT.txt`（サイドバーの文字案）だけは MOD になっても使います**
> ── サイドバーはクライアントが直接描くので、MOD でも絵にできないためです。
> **納品物は消していません**（`resourcepack/codex/`）。

### B. 中央プラントの建築 ── **ユーザーの作業**
4段階ぶんをユーザーが建て、そのあと構造物として保存して差し替える。

### C. 参加者向けのルールブック ── **未着手**
`README.md` は運営向け。参加者が読む物がまだ無い。

### D. 実機確認 ── **ユーザーの作業**（§4 の 1〜9）

### E. 値段の確定 ── **ユーザーの判断**
防具と弾の値段は**暫定**。変えるなら
`plugin/src/main/java/jidai/Shop.java` の `IPPAN` / `JUKIHIN` の該当行だけ。
**値段を変えたら `plugin/tests/harness/ShohinTest.java` と `BukiTest.java` の
期待表も一緒に直す**（直さないとテストが落ちる）。

---

## 7. 直近で分かったこと（2026-08-21）

### 検査が3本、1件も数えられていなかった
`YmlCheck` `CmdCheck` `LoadCheck` は `[PASS]` / `[FAIL]` の形で出しておらず、
走らせ役が1件も数えていなかった。
**plugin.yml が壊れていても全体は緑のまま通り抜ける状態だった。**

直したうえで、**わざと壊した .jar を5種類 作って落ちることを確認済み**
（plugin.yml 無し / main が実在しない / api-version 無し /
load: STARTUP / コマンド名に大文字と空白）。
これで **622 → 645**。

> **教訓**: 「その検査は、壊れている時に本当に落ちるか？」を先に確かめる。
> 落ちないなら、その検査は無いのと同じ。

### 文書の数字が古かった
`jikki_mod/手順.md` の表が「615」のままで、行も1つ崩れていた。両方 直した。

---

## 8. 用語

| 語 | 意味 |
|---|---|
| 勢力 | 丘陵 / 森林 / 川 / 内海 / 岩場 の5つ。番号 1〜5 |
| 中央 | 5勢力の中間。石油はここにしか湧かない |
| 時代 | 1=鉄器 / 2=中世 / 3=近代 / 4=現代 |
| 傭兵 | どの勢力にも属さない人。石油で雇われる |
| 下剋上 | 占領を跳ね返す唯一の手段（150・近代以降・1勢力1つ） |
| 先行ペナルティ | 中央より先へ進んだ勢力は光り、石油を失いやすくなる |

---

## 9. 関連するスキル

| スキル | 何が入っているか |
|---|---|
| **`mc-bukkit-plugin`** | **このプロジェクトの技術知識の正本。** プラグイン・Arclight・リソースパック GUI・1.20.1 の実測 |
| **`mc-forge-clientmod`** | **クライアント MOD 側の正本（2026-08-22 新設）。** Gradle 無しの Forge ビルド・SRG 名の対応表・チェスト画面の被せ方・紙＋CustomModelData の絵・Codex への絵の依頼と画素検査。`clientmod/` の道具（build / haichi / namae / mod_kakunin / tokushu_moderu / *_kakunin）の**汎用版**が同梱 |
| `mc-map-datapack` | マップ生成 / WorldPainter / バニラデータパック（1.21.10 時代の知識） |
| `drift-audit` | 実装と文書のズレを機械で洗う。**「完成」と言う前に通す** |
| `mc-minigame-recipes` | ミニゲームの企画案 |
